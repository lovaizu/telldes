# U-2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| ノードを1つも選択していない状態で Notes タブを開いたとき、note 設定済みレイヤーの一覧が表示されること | OK | 一覧は `selectionNote()` を見ない独立ブロックとして `src/App.tsx` の Notes パネルに置いた（選択中ノードのエディタを包む `<Show when={selectionNote()}>` の**外側・下**）。一覧の材料は `notes()` シグナルで、plugin 側が起動時に無条件で送る `notes-list` メッセージだけが書き込む（`handlePluginMessage` の `if (msg.type === "notes-list") handlers.setNotes(msg.notes)`）。選択が無い＝`selection-note` の `data: null` が来ても `setNotes` は呼ばれないことを `App.test.ts` の "keeps the list when the selection is cleared" で固定。DOM テスト環境が無いため JSX そのものは直接検証できず（依存追加は禁止）、描画に載る値＝`notes()` を純関数 `handlePluginMessage` 経由で固定する既存方針に従った |  |  |
| 一覧の各項目に、ルートからのレイヤーパスと note 本文の両方が表示されること | OK | ペイロード型 `LayerNote { nodeId, layerPath, note }`（`src/messages.ts`）。`layerPath` はページ直下から対象ノードまでの生のレイヤー名を ` > ` で連ねたもの。`code.test.ts` の "lists every noted layer on the page at startup, in layer-tree order" が `Home > Hero > Title` という3段の実値を固定し、"lists a note on a layer the export leaves out" が書き出し対象外（ページ直下の裸 COMPONENT 内部）でも `Button > Label` が出ることを固定。App 側は各項目で `item.layerPath`（`.result-node`）と `item.note`（`.note-list-text`, `white-space: pre-wrap`）の2行を描く |  |  |
| 一覧項目をクリックすると該当ノードが選択され、ビューポートがそのノードへスクロールすること | OK | 一覧の `<li onClick={() => selectNode(item.nodeId)}>` は Review タブと同じ既存ハンドラ `selectNode`（`src/App.tsx`）を呼び、既存の `select-node` メッセージを送る。受け側（`src/code.ts:124` 付近）は `figma.currentPage.selection = [sceneNode]` と `figma.viewport.scrollAndZoomIntoView([sceneNode])` を既に行っており、今回まったく変更していない（`git diff src/code.ts` の `select-node` ブランチに差分なし）。新しいメッセージ種別は作っていない |  |  |
| note を保存したあと一覧が再描画され、追加・変更・削除（空文字保存）が反映されること | OK | `save-note` ブランチが `note-saved` を post した直後に `sendNotesList()` を呼ぶ。`code.test.ts` の "resends the list once a note has been saved"（`notes-list` がちょうど2回 post され、2回目が保存した本文を含むこと）と "resends the list once a note has been deleted by an empty save"（空文字保存で最後の `notes-list` が `[]` になること）で検証。テスト用ノードは `setPluginData` で書いた値を `getPluginData` が返す可変スタブなので、「再送された一覧が保存結果を読み直している」ことまで通っている |  |  |
| note が1件も設定されていないページでは、空状態メッセージが表示されること | OK | `<Show when={notes().length > 0} fallback={<div class="note-list-empty">No notes on this page yet</div>}>`。plugin 側が0件ページでも `notes-list` を送る（＝UI が「まだ届いていない」と「0件」を取り違えない）ことを `code.test.ts` の "posts an empty list for a page holding no note" で固定（post 回数1・中身 `[]`） |  |  |
| 新たな問題が持ち込まれていないこと — (a) 既存テストが全グリーンであること | OK | `bun run test` → Test Files 14 passed / Tests 267 passed（着手時のベースラインは 257。差の10件はすべて今回追加分）。`bun run build` も UI・plugin code の両方成功し、`dist/code.js`・`dist/ui.html` の双方に `notes-list` が含まれることを確認。`dist/code.js` に `TextEncoder` / `fetch(` / `new Blob` / `atob(` は出現しない（新規 import した `export/layerPath.ts` は純粋な文字列操作のみ）。`bunx tsc --noEmit` は 82→97 件で、増分15件はすべて `src/App.tsx`（TS7026 55→67、TS2769 9→11、TS7006 4→5）＝追加した JSX 要素とその `(item) =>` 引数に由来する既存同種の solid-js JSX 型基盤未解決エラー。新しい種類の型不整合は出ていない（ベースライン値は作業ファイルを退避して HEAD 版で実測、その後復元して 267 passed を再確認） |  |  |
| 新たな問題が持ち込まれていないこと — (b) 選択中ノードの note 編集・保存の挙動が変わっていないこと | OK | `getSelectedNote` / `sendSelectionNote` / `selectionchange` ハンドラ / `save-note` の `setPluginData`・`setRelaunchData` 分岐 / `note-saved` の post はいずれも無変更（追加したのは `note-saved` post の直後1行 `sendNotesList();` のみ）。UI 側もエディタ部分の JSX・`saveNote`・`noteText`・`noteSaved` は無変更で、追加は一覧ブロックと `notes` シグナルのみ。`selection-note` と `note-saved` の既存挙動（新規選択で本文差し替え＋Saved 解除、他ノード宛ての遅延 ack を無視）を検証する `App.test.ts` の既存5件は改変せず全パス |  |  |

### 検証手法の適用状況（Method）

- **テストファースト**: 設計書 4.7.3 に仕様を書いたうえで、実装前に `code.test.ts` に5件・`App.test.ts` に2件の落ちるテストを追加し、6件の失敗（`bun run test` → 6 failed / 258 passed）を確認してから `messages.ts` → `layerPath.ts` → `code.ts` → `App.tsx` を実装した。
- **テストが実装を動かした例**: 起動時の `sendNotesList()` を入れた時点で、`page.children` が throw する既存テスト2件が「import 時に落ちる」形で赤くなった。これは起動時走査の例外が `figma.ui.onmessage` の代入まで巻き込み、**全タブが無反応になる**ことを意味する。テストが先に拾ったため `sendNotesList` を try/catch で封じ、専用テスト "keeps the rest of the plugin working when the page cannot be scanned" を追加した。
- **重複の排除**: レイヤーパスの祖先走査は `src/export/exclusions.ts` に同じものが既にあったため、`layerPathOf(node, segmentOf)`（`src/export/layerPath.ts`）として1箇所に出し、README 用（兄弟の `-N` 区別あり）は `readmePathOf` がセグメント命名だけを渡す形に変えた。同じ事実が2箇所に書かれている状態は作っていない。
- **カバレッジ計測**: カバレッジプロバイダ（`@vitest/coverage-v8` 等）は未導入で、依存追加は禁止のため `--coverage` は実行できない。代わりに変異テストで裏を取った。
- **変異テスト（8件投入・8件検知・生存0）**: 各変異は投入 → `bun run test` → 復元を1件ずつ自動化して実施し、最後に `git status` と 267 passed を再確認して全復元を確認済み。
  | 変異 | 検知したテスト（抜粋） |
  |---|---|
  | `src/code.ts` `if (!note) continue;` → `if (note) continue;` | notes-list > lists every noted layer on the page at startup, in layer-tree order ほか5件 |
  | `src/code.ts` 起動時の `sendNotesList();` を削除 | notes-list > posts an empty list for a page holding no note ほか4件 |
  | `src/code.ts` `save-note` 後の `sendNotesList();` を削除 | notes-list > resends the list once a note has been saved / … deleted by an empty save |
  | `src/code.ts` `layerPath: layerPathOf(...)` → `node.name` | notes-list > lists a note on a layer the export leaves out ほか3件 |
  | `src/code.ts` `sendNotesList` の try/catch を撤去 | notes-list > keeps the rest of the plugin working when the page cannot be scanned / run-checks > reports a check failure… / run-export > reports an export failure… |
  | `src/App.tsx` `if (msg.type === "notes-list") handlers.setNotes(msg.notes);` を削除 | handlePluginMessage > shows the page's noted layers when the plugin sends the list |
  | `src/export/layerPath.ts` `names.unshift(...)` → `names.push(...)` | layerPathOf > walks up to the page… ほか collectExclusions 系8件 |
  | `src/export/layerPath.ts` 終端条件から `DOCUMENT` を削除 | layerPathOf > stops at a DOCUMENT too… / layer paths (via findColorStyleUsage) > stops at a DOCUMENT parent… |
- **実機未確認**: Figma 上での実行はこの環境からできないため、実機確認は未実施。代替として両ビルドの成功と、`dist/code.js` / `dist/ui.html` の双方に `notes-list` が含まれること、および plugin サンドボックスに無いグローバル（`TextEncoder` / `fetch` / `Blob` / `atob`）が `dist/code.js` に現れないことを確認した。
- **未テスト（自動テストなし）**: `src/App.tsx` の JSX 描画そのもの（一覧の並び・空状態の出し分け・クリックハンドラの結線）。DOM テスト環境が未導入で依存追加が禁止のため、描画に載る値を純関数側で固定する既存方針に従った。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | NG | plugin 側（`code.ts` / `layerPath.ts`）は変異で堅く固定されている一方、**UI 側 `App.tsx` は完全に無防備**。次の変異がすべて 267 passed のまま生存: 一覧ブロックを `<Show when={selectionNote()}>` で包む（条件1が破れる）／`{item.layerPath}` → `{item.note}`（条件2）／`onClick` 削除（条件3）／`when={notes().length > 0}` → `=== 0`（条件5）。セルフチェックが条件1の根拠に挙げる "keeps the list when the selection is cleared" はシグナル値しか見ておらず、本当のリスク（`<Show>` の内側に置く）を構造的に捉えられない。「DOM 環境が無い」は事実だが、リポジトリの既存方針「描画に載る値を純関数に切り出して固定する」が**新規コードに適用されていない** — 行データの組み立ても空状態の判定も純関数化されておらず、方針を引用しただけで実行していない |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | NG | 一覧は **UI 側が保持する初めてのページ依存の永続状態**。Review / Export は毎回 `figma.currentPage` を読み直すので無縁だったが、一覧は起動時に1度作られたきりセッション中残る。設計書 4.7.3 がこの性質を扱っていないのが根本。`sendNotesList` の `catch {}` が要る理由（起動走査が `figma.ui.onmessage` 代入より前に走る）は**順序の副作用であって不可避ではない** — 起動走査を代入の後ろへ移せば封じ込め自体が不要になり、失敗を正直に通知できる。`layerPathOf` の切り出し粒度（可変部分だけコールバック）は妥当だが、置き場所が `src/export/` のままなので **Notes タブという非 export 機能が `src/export/` に依存する**形になった（`src/util/` が中立な置き場として既にある） |
| System-wide integrity (interfaces, cross-doc consistency) | NG | 設計書 4.7.3 追記の**文面自体が矛盾**: 「現在のページで note が設定済みの全レイヤー」と宣言しながら、更新タイミングを「起動時と note 保存時」だけと定めている。Figma はプラグインを閉じずにページを切り替えられるので両立しない。さらに走査範囲をページ全体へ広げた帰結が未処理 — 書き出し対象外レイヤーの note は `spec.json`（`specBuilder.ts:405-407` は対象フレーム配下のみ）にも README 除外物セクションにも載らないのに、一覧にだけ出る。「一覧に出ている＝CC に届く」と読めて届かない、4.3.4 に反する新しい黙殺（4.7.2 に「書き出し対象外レイヤーに付いた note」を除外物として追記すべき。実装は U-3 へ） |

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG | plugin 側の作りは堅い（投入変異10件すべて kill、`layerPathOf` 切り出しも `readmePathOf` の変異で9件落ちることを確認）。落としたのは2点。(1) `currentpagechange` 未購読でページ切替後の一覧が別ページのものになり、その行をクリックすると `figma.currentPage.selection = [node]` が throw するが `select-node` は async ハンドラ内で try が無く**未処理 rejection＝UI に何も届かない**（実測: エラー系 post 0件）。削除済みレイヤーだと `getNodeById` が null を返して素通り＝無反応。(2) `catch {}` のコメントが挙げる理由は**2つの呼び出し元のうち起動時にしか当たらない** — 保存時は `onmessage` 代入済みで報告手段が生きているのに同じ沈黙が適用される（実測: 保存直後に走査が失敗すると post は `note-saved` のみ、UI は緑の Saved を出しつつ一覧は古いまま）。既存の `fetchVariablesAndTextStyles` の swallow は「API が無いファイル種別」という既知理由があり README 除外物に載る経路があるので性質が違う。小: `"note"` マジック文字列が2→3箇所、`layerPathOf` のコールバック引数名が外側の `node` をシャドウ、0件時に「Notes on this page (0)」と「No notes on this page yet」で同じ事実を2回言う |
| Consistency with existing style | OK | コメント密度は実測 14/101 = **13.9%**（`code.ts` 6/34、`App.tsx` 1/31、`messages.ts` 2/12、`layerPath.ts` 5/17、`exclusions.ts` 0/7）で既存の帯 5〜25% の中ほど。内容も型・コードから読めない「なぜ」に限られている（走査範囲がページ全体である理由、コールバックを受ける理由、`white-space: pre-wrap` の理由など）。**問題はコメントの量ではなく `code.ts:71` の内容が事実と違うこと** |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 隔離コピーで 267/267 実測、両ビルド成功。指示した必須変異7件はすべて kill。GWT の形は概ね守られ、`it` 名が挙動を述べる文になっている。`setPluginData` の書き戻しを `getPluginData` が読み直す可変スタブなので、再送が保存結果を読み直していることまで通っている |
| Coverage (edge cases / claims / steps) | NG | 3つの穴。(1) **条件3（クリックで選択＋スクロール）にテストが1本も無い** — `scrollAndZoomIntoView` の行を丸ごと削除しても 267 passed。`select-node` ハンドラは純粋な plugin 側ロジックでテスト可能（プローブで実証済み）で、書けるのに書いていない。(2) **「生のレイヤー名をそのまま」が固定されていない** — テストのレイヤー名が `Home` / `Hero` / `Title` と全部 ASCII なので、4.5.2 相当の正規化（`name.replace(/[^A-Za-z0-9]+/g,"-")`）を混入させても誰も気づかない。設計書 4.7.3 が明記している性質なのに空振り。テストデータを `Hero / Top` や `見出し ブロック` に変えるだけで殺せる。(3) 描画部分（条件2・5）は DOM 制約の直接の帰結で、この軸では「未検証」と切り分ける以上のことはできない。未テストの実挙動: ページ直下1セグメント、同名兄弟が同一パスで並ぶ、名前に ` > ` や `/` を含む場合、6階層ネスト、INSTANCE 内部、空白のみの note。いずれも設計どおりだが固定されていない。また `save-note` に try/catch が無く（`run-checks` / `run-export` にはある）、`sendNotesList()` を足したことで壊れたときの道連れが1つ増えた |

## Overall Verdict

- Self-check: OK
- QA: **NG**
- Design expert: **NG**
- Craft expert: **NG**
- Verification expert: **NG**
- Ready to check off: **No** — 4軸とも fail。下記の未解決項目を U-2 の作業内容に畳み込んでから再判定する

## 4軸レビューの triage（2026-09-20）

4軸を独立サブエージェントで実施（対象 `56bef79`、各軸には差分・完了条件の逐語コピー・観点チェックリストのみを渡し、本ファイルは読ませていない）。今回は変異テストの相互汚染を避けるため、最初から `git archive HEAD` の隔離コピーで作業するよう指示した。

**4軸とも fail。** 指摘は3つの原因に収束する。すべて Valid（完了条件を直接破っているか、設計原則 4.3.4 に反する）。

| # | 指摘 | 一致軸 | 破れる完了条件 |
|---|---|---|---|
| N-1 | `currentpagechange` を購読していないため、ページ切替後の一覧が前ページのまま残る。その行をクリックすると `figma.currentPage.selection = [node]` が throw するが `select-node` は async ハンドラ内で try が無く**未処理 rejection になり UI に何も届かない**。削除済みレイヤーなら `getNodeById` が null を返して素通り＝無反応 | QA / Craft / Verification / Design（4軸） | 条件3 |
| N-2 | `sendNotesList` の `catch {}` が 4.3.4「黙って落とさない」に反する。(a) **起動時には「前回の一覧」が存在しない**（初期値 `[]`）ため、走査が失敗すると UI は「No notes on this page yet」という**嘘の空状態**を出す — `code.ts:71` のコメントが避けると言っているまさにその状態。(b) 保存時は `onmessage` 代入済みで報告手段が生きているのに同じ沈黙が適用され、「Saved は出るのに一覧に載らない」だけが残る | QA / Craft / Design（3軸） | 条件5、条件6b の趣旨 |
| N-3 | UI 側の描画が完全に無検証。一覧ブロックを `<Show when={selectionNote()}>` で包む／`{item.layerPath}` → `{item.note}`／`onClick` 削除／空状態の条件反転 — **どれも 267 passed のまま生存**。既存方針「描画に載る値を純関数に切り出して固定する」が新規コードに適用されていない | QA / Craft / Verification（3軸） | 条件1・2・5 |

### 単独軸の指摘（いずれも Valid）

- **N-4**（Verification）: 条件3 に自動テストが1本も無い。`scrollAndZoomIntoView` の行を削除しても 267 passed。`select-node` ハンドラは純粋な plugin 側ロジックでテスト可能で、書けるのに書いていない
- **N-5**（Verification）: 「生のレイヤー名をそのまま」（4.7.3）が固定されていない。テストのレイヤー名が全部 ASCII なので正規化を混入させても検知できない
- **N-6**（Design）: 設計書 4.7.3 追記の文面が自己矛盾（「現在のページの全レイヤー」vs「更新は起動時と保存時だけ」）
- **N-7**（Design）: 走査範囲をページ全体に広げた帰結が未処理。書き出し対象外レイヤーの note は `spec.json` にも README 除外物にも載らないのに一覧にだけ出る → 4.7.2 に除外物として追記（実装は U-3 へ）
- **N-8**（Design）: 一覧クリックが未保存の入力を無言で捨てる（`selection-note` が `setNoteText` で上書き）。従来どおりの挙動だが、編集中の入力欄のすぐ下に1クリックで捨てるコントロールが新設された点が新しい
- **N-9**（Design）: 主コンポーネント内部の note がインスタンス配置数ぶん重複して並ぶか未確認。**実機でしか確認できない**
- **N-10**（Craft/Design）: `layerPathOf` が `src/export/` にあるため、非 export 機能の Notes タブが `src/export/` に依存する。`src/util/` が中立な置き場として既にある
- **N-11**（Craft）: `"note"` マジック文字列が2→3箇所に増えた／`layerPathOf` のコールバック引数名が外側の `node` をシャドウ／0件時に同じ事実を2回表示
- **N-12**（Verification）: `save-note` に try/catch が無く（`run-checks` / `run-export` にはある）、`sendNotesList()` を足したことで壊れたときの道連れが1つ増えた

### 方法論についての記録

N-1・N-2 は **Figma 実機で1分触れば出る**種類。N-3・N-4・N-5 はテストの穴なので実機では出ない。4軸レビューが独自に稼いだのは実質後者で、それは「完成後に1回」で足りる。一方、先日の Export 失敗（`TextEncoder` がプラグインサンドボックスに無い）は U-4 の3ラウンド×4軸＝のべ12回のレビューを通り抜けた — 4軸はすべて Node で走るため原理的に捕まらない。**レビューの配置を見直す論拠として記録する。**

## 設計書更新（N-6〜N-8）のセルフチェック

対象: `docs/design.md`（4.7.2 / 4.7.3 / 4.7.5）と `README.md`（Notes 節・Export「書き出されないもの」）。文書のみ。`src/` は無変更。

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 4.7.3 に「現在のページの全レイヤー」と両立しない更新タイミングの記述が残っていないこと | OK | 旧「更新タイミングはプラグイン起動時と note 保存時」を削除し、`docs/design.md:503`「更新タイミングはプラグイン起動時・note 保存時（空文字保存＝削除を含む）・ページ切り替え時」に置換。`grep -n "起動時と note 保存時" docs/design.md` は 0 件 |
| (a) 行クリック時に対象ノードが現在のページに無い場合の扱いが理由つきで書かれていること | OK | `docs/design.md:504`「行が指すレイヤーが現在のページに無いときは、その旨をUIに通知し、一覧を作り直す」。理由: 黙って無反応だとクリックが効かないのか対象が消えたのか分からない（4.3.4）／作り直すのは同じ行を再クリックしても同じ結果になる状態を残さないため |
| (b) 走査失敗時の扱いと「未受信」「0件」の区別が理由つきで書かれていること | OK | `docs/design.md:505`「一覧の走査が失敗したときも黙らない」: 失敗を UI に通知し、「一覧が届いていない」と「0件」を区別して表示。理由: 区別しないと走査失敗が嘘の空状態として見える（4.3.4）。`:507` の 0 件メッセージ箇条を「上記の区別の片側」として接続 |
| (c) 未保存入力が一覧クリックで捨てられる事実と理由が書かれていること | OK | `docs/design.md:508`「行クリックで未保存の入力は捨てられ、確認は挟まない」: 入力欄は選択中ノードの保存済み note を映す単一の源泉／保存ボタンが明示的コミット／行クリックは選択操作と同義／確認ダイアログは「1クリックで辿る」用途を壊す |
| 4.7.2 の除外物告知に「書き出し対象外レイヤーに付いた note」が項目として追加され、記録内容と走査範囲が書かれていること | OK | 項目: `docs/design.md:483`（何を→なぜ除外→何を記録: 対象レイヤーパスと note 本文）。走査範囲: `:491`「書き出し対象のページ直下 `FRAME`／`SECTION` 配下に無いノード」（裸 Component 定義の内部、ページ直下の裸の他のノードとその内部）。対象フレーム配下を走査しない理由も併記 |
| README の Notes 節が設計書と食い違っていないこと（更新タイミング・クリック時の挙動） | OK | `README.md:191` 更新タイミング「起動時・note 保存時・ページ切り替え時」＝ 4.7.3:503。対象が無いとき「その旨が表示されて一覧が更新される」＝ 4.7.3:504。作れなかったときの表示と 0 件の区別 ＝ 4.7.3:505。`README.md:193` 書きかけが消える ＝ 4.7.3:508。README は「何を」のみ、理由は書いていない |
| 設計書の他の節に、今回の変更と矛盾する記述が残っていないこと | OK | 4.7.1（README が「何を」の正）: 変更不要。4.3.4 表: トークン源泉の表であり note は対象外、基本姿勢を 4.7.3 から参照する形で整合。4.7.2:487「裸の Component 定義の内部は走査しない」は Color Style／Variable のカテゴリ限定の記述で、note 項目は別カテゴリとして `:491` に範囲を定めたので矛盾なし。4.7.4:511「裸の Component 定義は README の除外物へ記録」と整合。4.7.5 に `figma.on('currentpagechange')` 行を追加（`:532`）。`grep -n -i "note" docs/design.md` の他の行（4.3.7、4.5.2:348、4.8、4.10）は今回の変更に触れない |

### 出典（各主張をどこで確認したか）

- ページ切り替え時に一覧が古くなる／`currentpagechange` の存在: `node_modules/@figma/plugin-typings/plugin-api.d.ts:540-542`（"This event will trigger when the user navigates to a different page"）。現行 `src/code.ts:108` は `selectionchange` のみ購読しており `currentpagechange` は未購読
- 一覧の更新タイミングの現状（起動時・保存時）: `src/code.ts:106`（起動時 `sendNotesList()`）、`:152`（`save-note` 後）
- 行クリック時の現状（対象が無いと無反応）: `src/code.ts:131-138` `select-node` は `getNodeById` が null なら何も post しない。UI 側 `src/App.tsx:324` は Review と同じ `selectNode` → `select-node`
- 走査失敗時の現状（黙る）: `src/code.ts:63-73` `sendNotesList` の `catch {}`。UI 側 `src/App.tsx:316-319` は `notes().length` だけで 0 件表示を出し、「未受信」と区別できない
- 未保存入力が捨てられる仕組み: `src/App.tsx:86-90` `selection-note` 受信で `setNoteText(msg.data?.note ?? "")`。保存は `:201-205` の `saveNote` → `save-note` のみ
- 書き出し対象外レイヤーの note が zip に載らない: `src/export/specBuilder.ts:405-408` は `spec.note` を走査中のノードにのみ付け、走査は `src/code.ts:204-209` で書き出し対象フレームごとの mock page に限られる。除外物の収集 `src/code.ts:193-197` は `scope` のみを受け取り note を扱わない
- 除外物の既存4項目の書きぶり・走査範囲の線引き: `docs/design.md` 4.7.2（項目 `:479-482`、走査範囲 `:486-492`）。「書き出し範囲の外にあるものを告知するための項目」の先例は裸 Component の行（`:490`）
- 基本姿勢「暗黙のドロップ/スキップ禁止」: `docs/design.md:82`（4.3.4）。Review の実行失敗の扱い: `:476`（4.7.2）
- 書き出し範囲: `docs/design.md:511`（4.7.4）
- note が spec.json の `note` としてのみ届く: `docs/design.md:348`（4.5.2）

### 補足

- README の Export「書き出されないもの」の一覧にも「画面フレームの外に置いたレイヤーに付けた note」を1行足した（`README.md:217`）。4.7.2 の項目一覧と README のこの一覧は対応関係にあり、片方だけ増やすと食い違うため。検出の実装は U-3
- 4.3.4 の表は変更していない（トークン源泉の表であり、note は該当しない）

### 修正ラウンド1

対象: `docs/design.md`（4.7.2 / 4.7.3）と `README.md`（Notes 節）。文書のみ。`src/` は無変更。

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 4.7.3 に走査失敗時に前回の一覧を消すこと・3状態（未着／失敗／受信済み）・0件メッセージは受信済みでのみ、がそれぞれ理由つきで書かれていること | OK | `docs/design.md:505`「一覧の走査が失敗したときは、その旨をUIに通知し、前回の一覧を消す」: 理由＝一覧は直近1回の走査結果／ページ切り替え直後の失敗で前ページの一覧が今ページの一覧に見える（4.7.2 の Review 実行失敗で前回結果を消すのと同じ理由）。`:506`「一覧のUI状態は「未着」「失敗」「受信済み」の3つで、0件のメッセージは受信済みでのみ出す」: 3状態の定義と、未着・失敗を0件と同じ見た目にすると嘘の空状態になる理由（4.3.4）。旧 `:505`（「届いていない」）と旧 `:507`（「まだ届いていない」）の2箇条は削除し、`grep "届いていない"` は `:506` の3状態定義の1件のみ |
| 4.7.3 に、一覧が3時点のスナップショットで削除・undo・別セッションの変更を追わないことと理由が書かれていること | OK | `:503` 見出し「…3時点で作り直すスナップショットで、それ以外の変更は追わない」。本文: 削除・undo・別セッションの変更は追わない（`documentchange` は購読しない）／理由＝文書全体の変更通知は重く（dynamic-page では全ページのロードが前提）、変更が周期的にまとめて届くので購読しても常に最新とは言えない／古い行はクリック時の検出で回収、undo で note だけ消えた行はクリックすれば入力欄が空の保存済み note を映す＝許容する古さ／行クリックで選択が成立しても一覧は作り直さない |
| 4.7.2 の走査範囲で、裸の Component 内部の note を走査する理由が Color Style 使用を走査しない理由と衝突していないこと | OK | `:486` は「既に1件記録済みで二重計上」を Color Style／Variable 不走査の根拠とする。`:491` 末尾に「Color Style使用と違い、note本文はデザイナーが書いた情報そのもので『Component X は対象外』の1件から復元できない。二重計上ではなく別の事実として記録する」を追加し、両者が別の性質である旨を明示 |
| 4.7.2 の総則が note の記録内容（レイヤーパスと本文）を含んでいること | OK | `:478`「対象レイヤーパス（トークン名の衝突はトークン名、noteはレイヤーパスと本文）付きで記録する」 |
| 設計書・README に README への記録と食い違う表現が残っていないこと | OK | `:483` を「`spec.json` には現れない（CCがスペックとして読む場所には届かない）」に置換。`README.md:193` を「書き出したスペックには含まれない（zip の `README.md` に記録されるだけ）」に置換。`grep -n "どこにも現れない\|書き出しには含まれない" docs/design.md README.md` は 0 件 |
| 4.7.3 の未保存入力の箇条が「選択が変われば」の条件付きで、README の Notes 節と一致していること | OK | `:508`「行クリックは選択変更として扱い、選択が変われば未保存の入力は捨てられる。確認は挟まない」。本文も「入力が捨てられるかどうかは選択が変わったかで決まり、行クリック自体では決まらない」。`README.md:193`「選択が変わって書きかけの内容は消える」と同じ条件 |
| 設計書の他の節に、今回の変更と矛盾する記述が残っていないこと | OK | 4.3.7（`:127-129`）は note の受け皿と自由記述の理由のみ。4.5.2 `:348`「`note` は付与されたノードにのみ存在」は spec.json の話で整合。4.7.4 `:514` 裸 Component の README 記録と整合。4.7.5 `:532` の `currentpagechange` 行は 3 時点の1つで整合（`documentchange` は表に無く、4.7.3 の不購読と矛盾しない）。4.8 `:553,561,570`・4.10 `:591` は note の用途のみ。README `:145-147`（note 節）・`:219`（書き出されないもの）も変更後の文面と整合 |

出典:

- `documentchange` の重さと非同期バッチ: `node_modules/@figma/plugin-typings/plugin-api.d.ts:555-557`（dynamic-page では `loadAllPagesAsync` が必要、"may introduce a loading delay"）、`:569`（"will not call the 'documentchange' callback synchronously and will instead batch the updates … periodically"）、`:1895-1907`（`loadAllPagesAsync` "may be slow for large documents"）。`manifest.json` は `documentAccess` を宣言していないので、本文は「dynamic-page 設定下では」と条件付きで書いた
- `currentpagechange` は `selectionchange` を必ず伴う: 同 `:549`。現行 `src/code.ts:108` は `selectionchange` のみ購読
- 行クリック→選択→入力欄が保存済み note を映す: `src/code.ts:131-138`（`select-node` → `figma.currentPage.selection`）、`:27-40`（`selectionchange` で `getPluginData("note")` を送る）、`src/App.tsx:86-90`（`selection-note` 受信で `setNoteText`）。したがって「undo で note が消えたレイヤーの行をクリックすると入力欄は空の保存済み note を映す」は成り立つ
- 選択が変わらない場合に入力が残るか（同じノードの行をクリックしたとき Figma が `selectionchange` を発火するか）は typings に記述が無く裏取りできないため、設計書には「選択が変われば」の条件形だけを書き、「同じノードなら残る」とは書いていない
- 現行 UI の状態が2状態（`notes().length` のみ）であること: `src/App.tsx:316-319`。走査失敗を握りつぶす現状: `src/code.ts:63-73`
- Review の実行失敗の2規則（通知＋実行中解除／前回結果を消す）: `docs/design.md:476`
- 書き出し対象外 note が `spec.json` に載らないが README には記録される: `docs/design.md:483`（4.7.2 除外物項目）、`README.md:213-219`

## 設計レビュー（実装前、設計書 4.7.2 / 4.7.3 / 4.7.5 と README Notes 節の文面を対象。2026-09-20）

対象 `35ba5ed`。Design エキスパートに差分・完了条件の逐語コピー・観点チェックリストのみを渡した（本ファイルは渡していない）。

**1回目: fail。** 完了条件は文面上すべて満たすが、実装が2通りに読める箇所が2つあり、片方は 4.7.3 が塞ごうとした穴を再現する。指摘8件はすべて Valid と判定し、修正ラウンド1（`660759c`）で対応した。

| # | 指摘 | 対応 |
|---|---|---|
| 1 | 走査失敗時に前回の一覧を消すかが未定義。ページ切り替え直後の失敗で前ページの一覧が残る読みが成立する | 「通知して前回の一覧を消す」と明記 |
| 2 | 「届いていない」が失敗と未着の両方を指し、UI 状態が2か3か決まらない | 「未着」「失敗」「受信済み」の3状態、0件メッセージは受信済みのみと明記 |
| 3 | 許容する古さの境界が無く、`documentchange` を購読すべきか判断できない | 3時点のスナップショットで削除・undo・別セッションは追わない、と理由つきで明記 |
| 4 | 裸 Component 内部の note を走査する理由が、4.7.2 で Color Style 使用を走査しない理由（二重計上）と衝突して見える | note 本文は「Component X は対象外」の1件から復元できない別の事実、と明記 |
| 5 | 行クリックで選択が成立しても作り直さないため、undo で note が消えた行が残る | #3 の境界の中で明示的に許容 |
| 6 | 4.7.2 総則に note の記録内容（本文）が無い | 括弧内に追加 |
| 7 | 「zip のどこにも現れない」が README 記録後の状態と食い違う | 「`spec.json` には現れない」に言い換え。README も同様 |
| 8 | 「行クリックで捨てられる」が絶対文。既に選択中の行なら入力は残る | 「選択が変われば」の条件付きに |

**2回目（`660759c`）: pass。** #1〜#8 すべて解消。新規は3点: (a) 503 行目の `dynamic-page` への言及は本プラグインの前提ではない（任意の文言調整、見送り）、(b) 行の対象が無いときの通知は一覧の3状態とは別の一時的な告知で、「失敗」表示と取り違えない置き場にする（実装時の注意として work-order に載せる）、(c) `save-note` の対象ノードが消えている場合の無反応は同型の黙殺だが今回の変更範囲外（N-12 に含めた）。
