# B-0 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| ②の層が受け取るデータの形が設計書にあり、出力・チェック・付け替えのどれもそれだけで作れること | OK | 設計書 4.7.6「読み取りデータ」に最上位・ノード・Variable/コレクション/Style の3表。設計書を上から歩いた対応: **spec.json** `page`←`pageName` / `viewport.width`←frame `width` / `background`←frame `fills` / `name`・`type`（section/block/element）・`path`・`-N`・フォルダ名←`name`,`type`,`children` / `screenshot`←`children`（子を持つか）/ `note`←`note` / `layout.*`←`layoutMode`,`layoutWrap`,`primaryAxisAlignItems`,`counterAxisAlignItems`,`counterAxisAlignContent`,`padding*`,`itemSpacing` / `layout.sizing`（`FILL/HUG/FIXED`,`widthPx/heightPx`,min/max）←`layoutSizing*`,`width`,`height`,`min*/max*` / `layoutAlign`,`layoutGrow` / `fills`（SOLID/IMAGE/GRADIENT、`opacity`、`colorToken`、stop の `colorToken`）←`fills`（Paint と stop の `boundVariables` 込み）/ `text.*`（characters,fontSize,fontFamily,fontWeight,fill,fillOpacity,lineHeight,letterSpacing,textAlign,textCase,textDecoration,`*Token`）←`text` の区間と `textAlignHorizontal` / `typographyToken`←区間の `textStyleId`＋`textStyles.name` / `cornerRadius`（一律・角ごと）,`cornerRadiusToken`←`cornerRadius`,`*Radius`,`boundVariables` / `effects`（型・各値・`*Token`・非可視除外）←`effects` / `opacity`←`opacity`。**tokens.json** 名前・`$type`・値・エイリアス解決・書体判別（scopes）・light/dark・`$base`・CSS 変数名←`variables`,`collections`,`settings.darkMode` / `typography`←`textStyles`。**settings.json**←`settings`,`nodes`（フレーム id→フォルダ名）,`otherFrames`,`variables`（themeColor）。**README.md** Contents←②が作る撮影一覧と `nodes` / 除外物5種←`fillStyleId`（区間含む）・`paintStyles`、`boundVariables`＋`variables.resolvedType/scopes`、ページ直下の `type`、`variables.name` と `textStyles.name`、書き出し範囲外ノードの `note`。**prompt.md/steering.md**←`width`（viewport）と固定テンプレート。**撮影一覧**（screenshots/assets/-dark/site）←`nodes`,`fills`（画像を含むか）,`type`（ベクター）,`settings`。**チェック** 構造4種←`type`,`layoutMode`,`children`,`name`,兄弟の `name`,背景は `name`/`x`/`y`/`width`/`height`/`layoutPositioning` / サイジング←`layoutSizing*` / テーマ整合5種←`fills`,`strokes`,区間 `fills`,`effectStyleId`＋`effectStyles`,`paintStyles`,`boundVariables`,`variables.collectionId`,`collections.name`,`theme`,`settings.darkMode` / 付け忘れ2種←生の値と `variables` のモード値、`cornerRadius`,`width`,`height`,`boundVariables`,`variables.name` / 範囲←ページ直下 `type` / 起動時の復旧の促し←`theme` / 行クリック←`id`。**付け替え**（4.3.9）対象(1)←書き出し範囲のノードの `boundVariables`（塗り・線・影・stop・区間）、対象(2)←`paintStyles`,`effectStyles` の `boundVariables`、対の照合←`variables.name`,`collectionId`,`collections.name`、向き←`theme`。**最初の歩きで設計書に無かった3件は決定済み（da79a40）**: (a) 非表示ノード → `visible` で出力・撮影・アセット・チェックから外し、告知の集約に `mainComponentId`（インスタンスのみ）を追加 (b) 線 → `strokes` 一式（`strokeWeight`・`strokeTopWeight` 等・`strokeAlign`・`dashPattern`・`strokesIncludedInLayout`・`strokeStyleId`）を spec.json の `strokes`/`strokesToken` に対応 (c) Color Style → Paint Style（不透明度・`boundVariables` 込み）から `fills` トークン、ノードの `fillStyleId` から `fillsToken`。あわせて決めた Effect Style トークン → Effect Style から `effects` トークン、`effectStyleId` から `effectsToken`。4.7.6 の表はこれに合わせて直し、`未決` は残っていない（`grep -n 未決 docs/design.md` の結果は0件）。`assets-dark/` の判定（見た目が変わるか）はバインドから計算しても画像を比べても読み取りデータで足りるため、どちらにするかは画像の書き出しの実装で決める。API 名（`strokeStyleId`,`effectStyleId`,`layoutPositioning`,`topLeftRadius`,`codeSyntax`,`layoutSizingHorizontal`,`counterAxisAlignContent`,`textAlignHorizontal`、区間の `fontWeight`/`textCase`/`textDecoration`/`fills`/`textStyleId`/`fillStyleId`/`boundVariables`）は `node_modules/@figma/plugin-typings/plugin-api.d.ts` で実在を確認。**2回目（レビュー指摘の修正）**: 読み取りデータに `primaryAxisSizingMode`・`counterAxisSizingMode`・`counterAxisSpacing`（4.6 を全行照合）、`clipsContent`・ノードの `blendMode`・`absoluteBoundingBox`/`absoluteRenderBounds`、`componentPropertyReferences.visible`、`mainComponentId`/`mainComponentName`（typings 9991 行 `getMainComponentAsync()` から①が作る。ノードの欄ではない）、Paint/Effect をそのまま（`visible`・`blendMode`・`imageHash`・`gradientTransform`・`showShadowBehindNode`。typings 4410〜4540・4049 行で実在確認）、最上位 `images`（先頭12バイト）を追加。`figma.mixed` は `"mixed"`＋辺・角ごとの欄。②に付け替えの計画（代入する配列そのもの、Style を当てた箇所は除外）・`assets-dark/` の判定（バインドから）・塗り/線だけの画像・Setup の差を置き、4.7.6 の層の表に反映。Export 順を light→読む→チェック→作る→撮る（4.7.4.4・4.7.6）。4.7.5 に `setRangeFills`（typings 8828 行）・配列代入・`getMainComponentAsync`・`createRectangle` 等を追加。`strokesIncludedInLayout` は Figma ヘルプ「Use the horizontal and vertical flows in auto layout」を WebFetch で確認（only inside strokes are counted / by default inside strokes are included）。`-webkit-text-stroke` と `paint-order` は MDN paint-order で HTML テキストに stroke と fill の順が効くことを確認。DTCG 2025.10 を WebFetch（色は colorSpace/components、dimension は value/unit、`$root`、テーマの定め無し）し、準拠の主張を「骨組みを土台にし、違いを表で明記」に置き換え、`$base` を `$root` に。`grep -c 未決` 0件。**3回目（再レビュー指摘）**: サイジングを主軸・交差軸ごとの1つの規則に統一（4.3.3・4.5.2.1・4.6・prompt §6）。Figma ヘルプ「Use auto layout with CSS Flexbox in mind」を WebFetch で確認（Fill の兄弟は中身の広さで分ける＝border-box）し、`flex: 1 1 0`＋border-box が同じ分け方になることを計算で確認。`layoutAlign`/`layoutGrow` と `primary/counterAxisSizingMode` を出力・読み取りデータ・4.6 から外した。流れの外の子に `position`（制約ごとの写し方）と `rotate`（typings 6220 行: 左上中心・`atan2(-m10, m00)`）を追加し、読み取りデータに `relativeTransform`・`rotation`・`constraints`（typings 4324 行 ConstraintType）。`text.runs` と `link`（`hyperlink`）を追加し、読み取りデータの text 行を更新。OUTSIDE のテキストの線を不透明な単色の `text.fill` に限定。画像の塗りを `backgroundSize`/`Position`/`Repeat` にし、`rotation`・`filters`（typings 4354〜4469 行）・回転を含む `imageTransform` は塗りの画像に。`shadowProperty` の3行目を塗りの無いノード全般に。DTCG の違いの表から影の inset 行を削除。実機項目に線のあるノードの影の落ち方（それまでの規則つき）・サイジング・制約と回転・画像の置き方を追加。**4回目**: `position` に回転前の `width`/`height` を入れ（STRETCH は書かない・SCALE は %・他は px、Group の子と回転した子は px）、`position` を持つノードは `layout.sizing` を出さないことにした（寸法の出どころを1つに）。アセットの箱の寸法の出どころを明記。不透明度 < 1 の画像の塗りを塗りの画像（一時長方形の PNG）側へ移し、IMAGE 行から `opacity?` を削除。主軸 FILL の `min-width: 0` は `minWidth`/`minHeight` があればその値。prompt.md の `position`・Fills・Assets・サイジング表を同じ内容に揃えた。**5回目**: 中身で決まる軸（HUG、`textAutoResize` の WIDTH_AND_HEIGHT／HEIGHT。typings に実在）は `position` に寸法を書かない（絶対配置の縮みに任せる）。読み取りデータの text 行に `textAutoResize`。4.3.3 の FILL を最小サイズに合わせ、prompt の欠けた寸法の意味と sizing の例外を追記 | OK | QA 3回（初回・再・最終）。初回で読み取りデータに穴（Style/Variable の CSS 名衝突、非表示の塗り等）→ ac009ce で解消。最終は d879d2c まで確認 |
| テストの目的・仕組みが設計書にあること | OK | 設計書 4.7.7。目的（約束＝出力の形 4.5/4.8・チェック 4.7.2・付け替え 4.3.9 だけ）、仕組み（②に見本の読み取りデータを与え、ファイル中身・撮影一覧・チェック結果・付け替え計画を正解と丸ごと比べる）、見本の取り方（①の JSON 保存機能→見本ファイル→JSON とわざと入れたものの一覧をリポジトリに置く）、正解の作り方（出力を写さない）と更新（設計書変更時は正解を先に直す／見本変更時は差を1件ずつ設計書と照らす）、GWT、実機でしか確かめないもの7項目。ステアリング「Ph-9 実装の組み直し」のテスト方針・B-1/B-2 の作業内容と突き合わせて食い違いなし。2回目: 見本2（Setup なし・Variable なし）と、`settings`/`theme` だけ差し替えた入力（ダーク OFF・theme=dark・設定空）を追加。わざと入れたものの一覧を「約束 → 見本の要素」の対応表にすることを要件化。実機項目に strokesIncludedInLayout の辺ごとの効き方・線と子の重なり順・effects の並び・blendMode の範囲・数値変数の単位・②の配列代入でのバインド・一時長方形の画像と `absoluteRenderBounds` を追加 | OK | 4.7.7 に目的・仕組み・見本2つ・派生入力・約束→見本の対応表 |
| CSS 変数名の規則が設計書・`prompt.md`・Setup で一致していること | OK | 設計書 4.5.1 に規則（`codeSyntax.WEB` の `var(--名前)` から `--名前`、無ければ `/`→`-` に `--`、Text Style は常に後者、対は Light 側、重なりは確かめない）と置き場所 `$extensions["com.github.lovaizu.telldes"].cssVariable`、例2つ（通常・light/dark）と typography 例に反映。4.3.4 に Setup の `codeSyntax.WEB`、4.3.9・4.8.1・4.7.5 を追随。`src/templates/prompt.md` は tokens.json 節・`*Token`・手順1で「`cssVariable` の名前をそのまま使い、自分で組み立てない」。`src/setup/tokenFactory.ts:116-117` `webCodeSyntax` = `var(--${name.replace(/\//g, "-")})` → 取り出すと `--` ＋ `/`→`-` で既定の作り方と同じ名前になり一致（コード変更なし）。出典: Figma MCP ガイド naming-conventions.md「replace "/" with "-", wrap in var(--)」「var() wrapper is REQUIRED for WEB」、DTCG format「$extensions はトークンにも付けられ、キーは逆ドメイン表記を推奨」を WebFetch で確認。決定済み（da79a40）: `var(--…)` 形でない code syntax は書かれていないものとし、名前から作る。名前から作る規則は `/`・空白→`-`、小文字化、文字・数字・`-`・`_` 以外を除去、`-` を1つにまとめて先頭・末尾の `-` を除去、空なら `token`、先頭に `--`。Figma ヘルプ「Variables in Dev Mode」を WebFetch で再確認: 例 `🔵 Blue 0000FF`→`--blue-0000FF`（絵文字の除去・空白→`-`・`Blue`→`blue` が確かめられる）、同じ記事の `Thin-100`→`var(--Thin-100, 100)` と `0000FF` は大文字のまま＝小文字化の条件は資料から決まらないと設計書に明記し、4.7.7 の実機確認に追加。Setup の名前（tokenFactory.ts の定数: 英小文字・数字・`-`・`/` だけ）はどの手順でも結果が変わらないことを確認。typography はキーごとの名前（プロパティ1つに変数1つ、の規則で説明）、影は `box-shadow` の値1つ。2回目: Style 由来の名前にグループ名を前置（`--typography-…`/`--fills-…`/`--effects-…`）、`.`→`-`（`spacing/1.5`→`--spacing-1-5`）。重なりを確かめない理由を「主な源2つを潰した」に更新し、4.5.1 の自己矛盾を削除。`cssValue`（単位込みの値。number は scopes から px/%/単位なし、決まらなければ持たせず告知）を追加。prompt.md の tokens.json 節と手順1を `cssVariable: cssValue` に揃えた。Setup の変数名は英小文字・数字・`-`・`/` だけで `codeSyntax.WEB` と一致したまま（コード変更なし） | OK | 設計書 4.3.4/4.5.1・prompt.md・tokenFactory.ts webCodeSyntax が一致（Style はグループ名付き、`.`→`-` を追加しても Setup の名前では結果同じ） |
| 既存テストが残っていないこと | OK | `git rm -r` で src/__tests__・src/checks/__tests__・src/export/__tests__・src/setup/__tests__ の15ファイルを削除。`find src -name __tests__ -o -name "*.test.*"` が空。package.json の test を `vitest run --passWithNoTests` にし、`bun run test` が "No test files found, exiting with code 0"（終了コード0）、`bun run build` も終了コード0 | OK | find で 0 件、`bun run test` 終了コード0 |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective | OK | テストは設計書の約束だけを実物の見本で丸ごと比べる。実機でしか分からないものは 4.7.7 の一覧に回した |

## Expert Reviews

### Design Expert
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | 初回 NG（線・影・グラデーションの CSS 対応が一般の場合に一致しない、①に計算が残る）→ ac009ce・7c2e7e0・e1e598c・d879d2c で解消。最終レビュー pass |
| System-wide integrity | OK | Export の順（light→読む）、付け替えと Style、サイジングの食い違いを解消 |

### Craft Expert (writing)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 出力の正確さに関わる指摘（prompt.md の古い条件・`border` への誘導・4.8.1 の抜け）だけ反映。言い回し・用語の揺れ・箇条の長さは本質的でないため見送り |
| Consistency with existing style | OK | 同上 |

### Verification Expert (fact-check)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked | OK | typings・Figma ヘルプ・MDN・DTCG 2025.10 と照合。誤り（CENTER の線を padding に足す、DTCG 準拠の主張、inset の行）は修正済み |
| Coverage | OK | 大文字の扱い・影の並び順・線と影の関係などは資料で決まらず 4.7.7 の実機確認に回した |

## Overall Verdict

- Self-check: OK
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: No（レビュー後の直しは4回。レビューと直しの繰り返しの上限3回を、最終レビューで見つかった退行1件の修正のため1回超えた。B-0 の手順「ユーザーが設計書の中身を確認する」が残る）
