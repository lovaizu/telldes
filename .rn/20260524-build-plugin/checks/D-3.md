# D-3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 上記の各点が `docs/design.md` に記載され、ステアリングの「確定:」3節が設計書を指すだけの状態になっていること | OK | 作業内容12項目の反映先: (1)「1フレーム＋トグル」と3コレクション → 新設 4.3.9（表・変数名にテーマを入れない・対を持つのは色だけ・複製方式を採らない理由3点・代償・`scopes: []`）。(2) 原則3つ → 4.7.2「チェックの原則」（原則1/2/3、判定軸の訂正を含む）。(3) Effect Style の影の色の行 → 4.7.2 原則1の表 第2行＋4.3.4 に根拠段落を新設（D-2 の未決4結論は 4.3.4 に未反映だったため補った）。(4) 書き出し手順 → 4.7.4.4。(5) 起動時 dark 残り → 4.7.2 末尾（error ではなく復旧の促し）。(6) 依頼書の節 → 4.7.4.1（5要素の表・切り分けと基準・LLM を持たない制約）。(7) 設定4群 → 4.7.4.2。(8) 保存先とタブの並び → 4.7.4.3。(9) favicon/OG → 新設 4.5.3 ＋ 4.5 の zip ツリー。(10) `spec.json` への載り方と CC の読み方 → 4.5.2.1「settings」＋ 4.8.1 の読み方の表。(11) 4.9 書き換え。(12) `src/templates/steering.md` から #3/#9/#10 を削除 ＋ 4.8.2 に反映。 | | |
| チェック体系の各チェックについて、ダーク対応ファイル／ライトのみファイルでの発火有無が表として設計書にあること。発火の判定軸が Export 設定であり Dark コレクションの有無でないことが明記されていること | OK | 4.7.2 原則1に6行の表（列: ダークモード対応 ON / OFF）。直前に「判定軸は Export 設定であって、Dark コレクションの有無ではない」を太字で置き、Setup が3コレクションを全ファイルに生成するという根拠（4.3.4 / 4.3.9 参照）を添えた。同じ判定軸を `tokens.json` 側にも適用し、4.5.1 末尾の「Darkコレクションが存在しないファイルでは常に単一値」という旧記述（有無を軸にしていた）を Export 設定基準に書き換えた。 | | |
| 設定4群それぞれについて、入力欄の形・保存先・`spec.json` への載り方・CC の読み方が設計書から追えること | OK | 入力欄の形 → 4.7.4.2 の表（4群×入力欄）。保存先 → 4.7.4.3（`figma.root.setPluginData`、ファイル単位、次回の既定値）。`spec.json` → 4.5.2.1「settings（Export 設定）」に JSON 例と各フィールドの規約（`responsive` の3項目、`themeColorToken`、`favicon`/`ogImage` のパス、全フレームに同じ `settings` が載る理由）。CC の読み方 → 4.8.1 の表（`responsive` / `darkMode` / `site` / `rules`）。権威記述は 4.7.4.2 に1本化し、4.5.2.1 と 4.8.1 はそこを指す形にして二重記述を作っていない。 | | |
| 4.9 に「描かれていない幅の振る舞いは推測しない」という原則と、その根拠（同一描画になるため区別不能）が記載されていること | OK | 4.9 の第4段落を「**描かれていない幅の振る舞いはデザインに存在しない情報であり、ツールが推測してはならない**」の太字で始め、根拠として `padding: 0 340px` と `max-width: 760px` + `margin-inline: auto` が 1440px で完全に同一の描画になること、差が出るのはデザインが記述していない幅だけであることを書いた。続く段落でコンテンツ幅の宣言により padding が「コンテナを作っていたもの」だと確定する導出を書き、角丸 `full`（4.3.6）が同じ形の問題であることに繋げた。 | | |
| `steering.md` テンプレートの確認項目と telldes の設定項目に重複が無いこと | OK | `src/templates/steering.md` の確認項目を10件→7件にし、レスポンシブ（旧#3）・OGP/meta（旧#9）・言語（旧#10）を削除して番号を振り直した。表の直前に「Responsive breakpoints, OGP / meta information and page language are **not** asked here — read them from `spec.json` の `settings`」の注記を追加。Completion Checks の `Responsive behavior works (if applicable)` は `settings.responsive` を参照する文言に更新。残る7件（出力形式・CSS方針・viewport幅・画像パス・分割粒度・フォント読み込み・デプロイ先）は 4.7.4.2 の設定4群のどの項目とも重ならない。設計書側は 4.8.2 に「確認項目と Export 設定に同じことを置かない」として理由付きで記載。 | | |

## 照合の記録（方法の適用）

各記述をその場で出典に当てながら書いた。照合対象は次のとおり。

| claim | 照合先 | 結果 |
|---|---|---|
| コレクション数に制限が無い／モード数に制限がかかる、3コレクションの内容、変数名にテーマを入れない理由、対を持つのは色だけ、複製方式を採らない理由3点、代償、`scopes: []` | steering「確定: ダーク対応は「1フレーム＋トグル」方式（2026-09-19）」 | 1対1で対応。方針の追加なし |
| 原則1/2/3の文面、発火の表（5行）、ゲートの表、判定軸の訂正、起動時 dark の扱い | steering「確定: チェックは体系として設計する（2026-09-20）」 | 1対1。表は Effect Style の行を足して6行にした（D-3 の作業内容の指示どおり） |
| 依頼書5要素の表、切り分けと「誰が答えられるか」、LLM を持たない制約、設定4群、専用欄と共通ルールの線引き、共通ルール⇔note の対、保存先、レスポンシブ設定が解く問題、角丸 `full` | steering「確定: Export は実装への依頼書（2026-09-21）」 | 1対1 |
| favicon は SVG / ICO / apple-touch-icon 180×180、OG 画像 1200×630 | steering「調査の出典（2026-09-21）」 | 一致 |
| `figma.root.setPluginData` が存在する | `node_modules/@figma/plugin-typings/plugin-api.d.ts` L453（`readonly root: DocumentNode`）、L9210（`DocumentNode extends BaseNodeMixin`）、L5354（`BaseNodeMixin extends PluginDataMixin`）、L5535（`setPluginData`） | 実在を確認 |
| `VariableBindableEffectField` は `color` / `radius` / `spread` / `offsetX` / `offsetY` | 同 L5821 | 一致（設計書に書いた5つと同一） |
| `Variable.scopes` が存在する | 同 L10644（`scopes: Array<VariableScope>`） | 実在を確認 |
| Setup が使う API（`createVariableCollection` / `setValueForMode`）と付け替えの API（`setBoundVariable` / `setBoundVariableForPaint` / `setBoundVariableForEffect`） | 同 L2132 / L8020 / L5700 / L2167 / L2179 | すべて実在。4.7.5 の表に追加した名前はこの5つと `figma.root.setPluginData` のみ |
| ICO は `exportAsync` で出せない | 同 L4625-4630（`ExportSettingsImage.format: 'JPG' \| 'PNG'`）、L4698 SVG、L4760（`ExportSettings` は Image / SVG / PDF の3つ） | ICO は無い。4.5.3 に「PNG を ICO コンテナに収める」と書き、生成手段は実装タスク送りにした |
| 実寸での書き出しが可能（180 / 1200） | 同 L4618-4620（`ExportSettingsConstraints.type: 'SCALE' \| 'WIDTH' \| 'HEIGHT'`） | 可能。ただし等比なので切り抜きは起きない → 4.5.3 に「切り抜きも余白の追加もしない」と根拠付きで明記 |
| favicon / OG が既存の書き出しの仕組みに乗る | `src/export/screenshotExporter.ts` L26-29、`src/export/assetExporter.ts` L59-79 | どちらも `node.exportAsync` に `constraint` を渡すだけ。違いは `SCALE` か `WIDTH` かだけであることを確認し、4.5.3 にそう書いた |
| zip ルートに `site/` を足すと衝突しうる | `src/export/zipBuilder.ts` L46-64（`root.folder(frame.folderName)`）、`src/export/__tests__/exportScope.test.ts` L101（予約名に `-2` が付く既存テスト） | 衝突する。4.5.2 の予約名集合に `site` を追加し、設定の有無に関わらず予約する理由を書いた |
| Effect Style のフィールド単位バインドが 4.3.4 に書かれている（と原則1の表から参照できる） | `docs/design.md` 4.3.4 を grep | **書かれていなかった**（D-2 の未決4結論のうち Color Style 分だけが反映済み）。4.3.4 に段落を追加して参照先を実在させた。あわせて 4.5.2.1 effects の「Effect Style を named token として出すのは未実装」がフィールド単位のトークンと別の話であることを明示し、矛盾を潰した |
| 既存テストが壊れていないこと | `npx vitest run` | 14 files / 267 tests すべて pass。`npx tsc --noEmit` のエラーは `src/export/specBuilder.ts` と `src/ui.tsx` の既存のもので、今回変更した2ファイルとは無関係 |

## 4軸レビュー

本タスクは4軸レビューを回さない。作業ルール「レビューの配置」により、**設計レビューは設計書を最新化する段の締めとして D-4 で1回**かける。D-3 の成果物（設計書の文面）がそのまま D-4 の対象になるため、ここで当てても二重になる。

## コーディネーターレビュー

| 観点 | 判定 | 根拠 |
|---|---|---|
| コミット差分が D-3 の範囲に収まっているか | OK | `914f781..d3e0c00` は `docs/design.md`（+206/-16）と `src/templates/steering.md`（+12/-5）のみ。実装コードに変更なし |
| 作業内容12項目が漏れなく反映されているか | OK | 12項目それぞれの反映先を上表で確認。新設は 4.3.9 / 4.5.3 / 4.7.4.1〜4.7.4.4、書き換えは 4.5.1 / 4.9 / 4.10 / 4.8.2 |
| 権威記述が1箇所にあり二重記述が無いか | OK | 設定4群の定義＝4.7.4.2、JSON上の形＝4.5.2.1、CC の読み方＝4.8.1。後2者は 4.7.4.2 を参照するだけ |
| 節の配置が既存の流れを壊していないか | OK | 4.7.4 を繰り上げず 4.7.4.1〜4.7.4.4 のサブ節にした判断は妥当。`4.7.4` は `src/code.ts` 等から参照されており、繰り上げるとスコープ外の実装変更になる |
| 完了条件1件目「ステアリングの確定3節が設計書を指すだけの状態」 | OK（コーディネーターが実施） | ステアリングはコーディネーターだけが書くため実装エキスパートの範囲外だった。D-3 のチェックオフと同じコミットで3節を設計書へのポインタ＋セッション上の経緯だけに縮めた |
| 設計書内に新たな矛盾を持ち込んでいないか | NG（D-4 送り） | 3件を検出。いずれも D-3 が作ったものではなく既存の食い違いが顕在化したもの。ステアリングの未決3〜5 に起票済み。最も重いのは **4.3.4「この32個を Setup が一式生成」と 4.7.2「Setup は3コレクションを一式生成するので Dark は全ファイルに存在」が両立しない**件で、原則1の判定軸の訂正の根拠そのものに関わる |
| 実装者が独断で決めた点が可視化されているか | OK | 8件をステアリング「D-3 で実装者が決めた点」に列挙し、D-4 の作業内容で1件ずつユーザー確認する形にした |

## Overall Verdict

- Self-check: OK
- QA / Design / Craft / Verification: N/A（D-4 の設計レビューで1回当てる）
- コーディネーターレビュー: OK（検出した矛盾3件は D-3 の成果物の瑕疵ではなく、未決3〜5 として D-4 で決着させる）
- Ready to check off: Yes
