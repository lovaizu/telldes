# R-1 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 各フレームがフレーム名のフォルダに出力されること | OK | トップレベルフレームごとに frame.name でフォルダ作成し、spec.json + screenshots/ + assets/ を配置 |
| tokens.json がルートに1つだけ出力されること | OK | tokens.json はルートに配置、フレームフォルダには含まない |

## 総合判定

- セルフチェック: OK
- ユーザーレビュー可否: 可
