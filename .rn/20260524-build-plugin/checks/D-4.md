# D-4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `docs/design.md` 4.3 の冒頭に原則A、4.5 の冒頭に原則B（4行の表つき）が置かれ、以降の節がそれを根拠に参照できる状態になっていること | OK | 4.3 冒頭に「**原則A — Figma 公式のベストプラクティスをそのまま土台にし…**」の段落（根拠1文＋原則Bの所在）。4.5 冒頭に「**原則B — 出力（CC が読むもの）は LLM が迷わない形にする。**」＋ステアリングの4行の表（形が揃っている／1箇所にだけある／欠けていると分かる／推測させない）＋適用範囲の1文。参照に寄せた既存記述: 4.3.4 源泉表の導入（「原則A に従い」）・書体の段落（原則A／原則B「形が揃っている」「推測させない」）・4.3.6 の `radius/full`（原則A）・4.5.1 の light/dark ペア（形が揃っている）・4.5.1 の `$type`（形が揃っている）・4.7.2 の除外物告知（欠けていると分かる）・4.7.4 の別ページ方針（原則A）・4.7.4.3 の note 一覧（欠けていると分かる）・4.9 の推測禁止（推測させない）。 | | |
| 未決1（note 一覧）が反映されていること | OK | 4.7.4.3 に段落「**Export タブの note 一覧は、走査範囲を書き出し対象フレーム配下に限る。**」を追加。理由（対象外レイヤーの note が「これから渡すもの」として並ぶと 4.7.2 が塞いだ黙殺を UI 側で作り直す）と、2つの一覧が走査範囲を引数に取る1つの関数を共有することを明記。4.7.3 は変更せず、該当箇条に Export タブとの範囲差の相互参照だけ足した。 | | |
| 未決3（Setup の生成数）が反映されていること | OK | 4.3.4 の生成数を「**変数32個（Color Light 8 / Color Dark 8 / Spacing 8 / Radius 5 / Font family 3）＋ Style 10個（Text Style 8 / Effect Style 2）＝ 42個**」に書き換え。直後に「**ダーク用の8色も Setup が最初から作る。**」の段落を新設し、根拠（`scopes: []` で隠れる／生成のタイミングが1つ／4.7.2 原則1 の判定軸の根拠が実在する）を書いた。トークン体系のブロックの Color 行も「Light 8 ＋ Dark 8」に更新。Setup 完了時メッセージは設計書に記載が無いため対象なし（ステアリングと README 側の話）。 | | |
| 未決4（書体）が反映されていること | OK | 4.3.4 源泉表に「**Variables（STRING）のうち書体用にスコープを絞ったもの** / font-family / `$type: "fontFamily"`」の行を追加し、対象外の行を「Variables（BOOLEAN）、それ以外の Variables（STRING）」に変更。直後に根拠の段落（原則B「形が揃っている」／Figma 公式の適用先／W3C DTCG）を追加。**判別方法は第2ラウンドで修正**（下記）。4.7.2 の除外物告知リストの当該行と走査範囲・集約の記述も「対象外Variable」に書き換え。4.5.1 の出力条件と `$type` の説明、JSON 例に `font.heading`（`$type: "fontFamily"`）を追加。4.5 の zip ツリーの `tokens.json` 行も更新。 | | |
| （第2ラウンド）書体 Variable の判別が誤検知しないこと | OK | 4.3.4 に段落「**書体かどうかは Variable の `scopes` で決める — `FONT_FAMILY` を含み、かつ `ALL_SCOPES` を含まないもの**」を新設。新規 Variable の既定が `ALL_SCOPES` であること、`ALL_SCOPES` を印にするとテキストの中身用の STRING まで書体として出てしまうこと、既定値のままは宣言ではない（原則B「推測させない」）、絞っていないものは除外物として README に載るので黙って落ちない（原則B「欠けていると分かる」）、を記載。**Setup が `font/heading` / `font/body` / `font/accent` に `scopes: ["FONT_FAMILY"]` を設定する**ことと、書体がトークンとして出ることがこれに依存することを明記。4.7.5 の Setup の API 行にも `variable.scopes`（書体は `["FONT_FAMILY"]`。4.3.4）を追記。4.7.2 の除外物告知の行は「書体として扱われない STRING Variable → トークン出力対象外（判別は 4.3.4）」とし、判別規則を1箇所に置いて読み合わせても矛盾しない形にした。設計書内の「書体名の STRING」という表現も「書体の STRING」に統一（4.5 ツリー・4.5.1 の2箇所）。 | | |
| 未決5（Typography 8個）が反映されていること | OK | 4.3.4 のトークン体系ブロックを「Typography（Text Style名、8）display / heading-lg / heading-md / heading-sm / lead / body / label / caption」に変更し、8段階である根拠（実測 18 / 20px が6段階から外れた、`lead` と `label` の役割）を段落で追加。4.5.1 の推奨体系の列挙を同じ順序に揃えた。 | | |
| 未決12 由来の訂正（Base から文字サイズを外す）が反映されていること | OK | 4.3.9 のコレクション表の Base 行を「余白・角丸・書体」に変更。直後の「**対を持つのは色だけ**」の段落も同じ3つに直し、「文字サイズは Text Style（4.3.4）が持つものでコレクションには入らない」を追記。 | | |
| 未決7（テーマ付け替えの対象）が反映されていること | OK | 4.3.9 に2段落を新設。「**暗くなるのはページ全体。**」で対象2つ（(1) ページ上の全書き出し対象フレーム配下のノード、(2) ファイル共有の `PaintStyle` / `EffectStyle`）と根拠（サイト全体にかかる振る舞い／全フレームを撮る／ファイル単位の状態フラグと粒度が一致）。「**影とグラデーションの色も変わる。**」で根拠と実現手段（`ColorStop.boundVariables` が `readonly`・`setBoundVariableForPaint` が `SolidPaint` のみ → 配列を作り直して再代入）。「フレームは複製しない」冒頭の「選択フレーム配下」も削除。4.3.4 のグラデーション段落に「ダークモード対応 ON では stop の未バインドも error」を追加。4.7.2 発火表に「グラデーション stop の色が変数を指していない」行を追加し、Review のチェック項目とゲート表の「テーマ整合4種」を5種に更新。4.7.5 の API 表に再代入の注記とスタイル列挙の行を追加。 | | |
| 未決8（Export 開始時が dark）が反映されていること | OK | 4.7.4.4 の手順を「**light にする** → light 撮影 → Dark へ付け替え → dark 撮影 → light に戻す」に変更し、段落「**先頭の「light にする」は、押した時点が dark 表示だった場合に備える。**」で理由（同じ動作の延長／error でゲートを止めない理由は「起動時に dark のまま残っている」と同じ）と、書き出し後は light のまま残すことを書いた。 | | |
| 未決9（発火表の3項目）が反映されていること | OK | 4.7.2 発火表から「色の未バインド（OFF 時）→ README」「Text Style の font-size が生数値」「padding/gap が生数値・4の倍数でない」の3行を削除（OFF 列はすべて「発火しない」）。直後の段落で報告しない理由（生値でも `spec.json` に実値が載る／数十〜数百行で除外物告知が埋もれる／並べること自体が促しになる）を書いた。「4の倍数」は設計書から消滅（grep で0件）。代わりの「付け忘れの知らせ」は 4.7.2 末尾に新設（下記）。 | | |
| 未決12（`radius/full`）が反映されていること | OK | 4.3.6 に段落「**命名は自由だが、この名前だけは意味を持つ。**」を追加し、4.3.4 の「命名規約は自由」との関係（telldes が当てるのではなくデザイナーが宣言する）、名前を外すと宣言が黙って効かなくなること、そのとき Export タブで知らせること、error にしない理由、名前方式を変えない理由（原則A）を書いた。4.3.4 の「命名規約は自由」にも「（唯一の例外は `radius/full`。4.3.6）」を付けた。 | | |
| 未決9・12 の置き場所（Export タブの並び）が反映されていること | OK | 4.7.2 末尾に「**付け忘れの知らせ（Export タブ）:**」を新設し、2種類（同じ値なのにトークンを指していない／丸いのに `radius/full` を指していない）・error にしない・自動置換しない（4.3.4 の自動判定しない）・zip の README ではなく Export タブに置く理由（4.7.4.1 の切り分け）を記載。4.7.2 冒頭も「本節は3つの異なる出力を定める」に更新。4.7.4.3 の並びを「設定 → 共通ルール → note 一覧（読み取り専用）→ 付け忘れの知らせ → Export ボタン」に変更し、ブロックしないことを明記。4.3.4 の「ツールは促さない」にも例外として1文を足して矛盾を消した。 | | |
| 未決10（画面でないもの）が反映されていること | OK | 4.7.4 の書き出し範囲の方針に段落「**画面でないものは、画面を置くページとは別の Figma ページに置いてもらう。**」を新設（対象3つ／現在のページだけを見るので自然に外れる／種類では見分けられない／印や除外リストを足さない／Figma 公式の `file > page > frame` 整理が根拠＝原則A）。4.7.4.2 に `documentAccess: "dynamic-page"` が無いため `loadAllPagesAsync` 不要という箇条を追加（`manifest.json` を実際に確認）。4.10 に「Starter チームのファイルは1ファイル3ページまで／Drafts は無制限（共同編集不可）」の行と、段落「**Free で成立するのは実質 Drafts 運用のとき。**」を追加。 | | |
| 未決11（ダーク用アセット）が反映されていること | OK | 4.5 の zip ツリーに `assets-dark/`（`images/` `icons/`）を `assets/` の隣に追加。4.7.4.4 に段落「**アセットもダーク用を書き出す。**」を新設し、根拠（SVG は色が焼き込まれる／色を抜く案は 4.3.4 の姿勢に反し多色ロゴに使えない）と「light と dark で見た目が実際に変わるアセットだけ2枚目を出す」理由（同一ファイルが2つ並ぶと CC が誤読する）を書いた。4.8.1 の「入力データの読み方」にも `screenshots-dark/` / `assets-dark/` を加えた。 | | |
| 未決13（Review の走査範囲）が反映されていること | OK | 4.7.2 冒頭の「ページ内の全ノードを走査して」を削除し、段落「**Review の走査範囲は書き出し対象の範囲**（ページ直下の `FRAME`／`SECTION` とその配下。4.7.4）」を新設。根拠（原則1 の判定軸は「出力が壊れるか」／zip に入らないものは出力を壊さない／ゲートの趣旨と逆）と、`src/export/exportScope.ts` の `isExportedFrame` を共有すれば定義上ズレないことを書いた。除外物告知の走査範囲の箇条にも「Review と同じ範囲・同じ判定」を明記。 | | |
| Setup の生成数が設計書全体で 変数32個 ＋ Style 10個 に揃っており、6個／25個／33個といった古い数値が1箇所も残っていないこと | OK | `grep -n "25 variables\|33個\|この32個\|6段階\|テーマ整合4種" docs/design.md` で0件。生成数の記述は 4.3.4 の1箇所のみ（42個＝変数32＋Style 10）。 | | |
| Text Style が設計書全体で8段階に揃っていること | OK | 4.3.4 のトークン体系ブロック（8個の列挙）と 4.5.1 の推奨体系の列挙が同じ8個・同じ順序（`display` / `heading-lg` / `heading-md` / `heading-sm` / `lead` / `body` / `label` / `caption`）。 | | |
| 4.7.2 の発火表に未決9 の3項目の README 送りが残っていないこと。かつ「4の倍数」の基準が定義なしで参照されている箇所が残っていないこと | OK | 発火表は5行すべて「error / 発火しない」で、README 送りの行は無い。`grep "4の倍数" docs/design.md` は0件。font-size・padding/gap は「報告しない」側の説明としてのみ残り、基準には触れていない。 | | |
| 4.7.2 冒頭の Review の走査範囲が「書き出し対象の範囲」になっており、除外物告知の走査範囲と矛盾しないこと | OK | 4.7.2 冒頭の新段落が「書き出し対象の範囲」。除外物告知の走査範囲の箇条は、Color Style／対象外Variable が同じ範囲（同じ判定を使うと明記）、Component 定義と対象外レイヤーの note は「範囲の外にあるものを告知する項目」として意図的に逆の範囲であることが元から書かれており、両者は矛盾しない。 | | |
| 4.3.9 のテーマ付け替えの対象が「ページ上の全書き出し対象フレーム配下のノード＋ファイル共有の `PaintStyle` / `EffectStyle`」になっており、4.7.4 の書き出し範囲（全フレーム）および 4.3.9 のファイル単位の状態フラグと粒度が揃っていること | OK | 4.3.9「**暗くなるのはページ全体。**」の段落に対象(1)(2)を明記し、「書き出しは全フレームを撮る（4.7.4）」「スタイルはファイル全体で共有されるものなので、下の状態フラグ（ファイル単位）とも粒度が一致する」と揃いを本文で示した。直後の「代償として、ファイルが「今どちらのテーマか」という状態を持つ」（`figma.root.setPluginData`）と同じ粒度。 | | |
| 4.3.9 の Base コレクションの説明に「文字サイズ」が含まれていないこと | OK | 表の Base 行は「余白・角丸・書体」。本文も同じ3つで、文字サイズは Text Style 側だと明記。 | | |
| zip の構成（4.5）に `assets-dark/` が含まれ、`screenshots-dark/` と並んでいること | OK | 4.5 の zip ツリーで `screenshots/` → `screenshots-dark/` → `assets/` → `assets-dark/` の順に並ぶ。 | | |
| 4.10 に Starter チームの1ファイル3ページ制限と Drafts 無制限、および「Free で成立するのは実質 Drafts 運用のとき」という条件が記載されていること | OK | 4.10 の表に該当行を追加し、表の直後に段落「**Free で成立するのは実質 Drafts 運用のとき。**」を置いた。代償（共同編集不可）が 1. 状況の前提（デザイナーは自分ひとり）と衝突しないことも書いた。 | | |
| 新たな矛盾が持ち込まれていないこと — (a) 変更した節を参照している他の節の記述が古くなっていないこと、(b) 節番号の相互参照がすべて実在する節を指していること | OK | (a) 変更に伴って追随させた箇所: 4.3.4「ツールは促さない」に付け忘れの例外、4.5.2.1 の `gradientStops` に `colorToken?`（4.3.4 の stop トークン出力と揃える）、4.5.1 の出力条件と `$type`、4.7.2 のゲート表・チェック項目（テーマ整合5種）、4.7.2 除外物告知の集約・走査範囲の文言、4.7.3 の note 一覧に Export タブとの範囲差の参照、4.7.5 の API 表、4.8.1 の入力データの読み方。(b) `grep -o "4\.[0-9]\+\(\.[0-9]\+\)*"` で抽出した参照先は 4.1〜4.10・4.3.1〜4.3.9・4.4.1/4.4.2・4.5.1/4.5.2/4.5.2.1/4.5.3・4.7.1〜4.7.6・4.7.4.1〜4.7.4.4・4.8.1/4.8.2 のみで、すべて実在する見出し。 | | |

## Overall Verdict

- Self-check: OK

## Fact-check log

| 主張 | 出典 | 結果 |
|---|---|---|
| 原則A／原則Bの文面と4行の表、既存の確定と矛盾しないこと | steering「確定: 判断は原則から導く（2026-09-21）」 | 文面をそのまま移した。表は4行とも一字一句同じ |
| 未決1・3・4・5・7〜13 の各結論と根拠 | steering「未決（この順に決める）」の各項 | 1対1で対応。設計書に無い方針は足していない |
| `ColorStop.boundVariables` が `readonly` | `node_modules/@figma/plugin-typings/plugin-api.d.ts` L4335-4350（`interface ColorStop` の `readonly boundVariables?`） | 一致。4.3.9 の実現手段の根拠として引用 |
| `setBoundVariableForPaint` が `SolidPaint` のみ | 同 L2167-2171（`setBoundVariableForPaint(paint: SolidPaint, …): SolidPaint`） | 一致 |
| `figma.getLocalPaintStylesAsync()` / `getLocalEffectStylesAsync()` が実在する | 同 L1501 / L1521 | 実在。4.7.5 の表に追加した名前はこの2つ |
| `VariableScope` に `FONT_FAMILY` がある（書体 Variable の判別に使える） | 同 L10453-10475 | 実在。`ALL_SCOPES` も同じ列挙にある。書体判別を `scopes` で行うと書いた根拠 |
| `scopes` は「変数ピッカーにどのフィールドで出すか」の絞り込みであって、バインドの可否ではない | 同 L10637-10644（`Variable.scopes` の doc comment: "An array of scopes in the UI where this variable is shown" / "does not prevent that variable from being bound in other scopes"） | 一致。第2ラウンドで「どのプロパティに適用できるかの宣言」という不正確な言い換えを削り、doc comment どおりの説明に直した |
| 新規作成した Variable の `scopes` の既定値は `["ALL_SCOPES"]` | コーディネーターの指摘（第2ラウンド）。型定義には既定値の記載が無い | 型定義では裏が取れない（`scopes` の既定値はドキュメント化されていない）。コーディネーターの指摘を採用し、設計書には「新規作成した Variable は既定で `ALL_SCOPES`（＝絞っていない）」と書いた。実機での再確認は未実施 |
| `manifest.json` に `documentAccess: "dynamic-page"` が無い | `manifest.json`（14行。`name` / `id` / `api` / `main` / `ui` / `editorType` / `relaunchButtons` / `networkAccess` のみ） | 一致。別ページのフレームにも同期アクセスできる |
| Review と書き出しが同じ関数を使える | `src/export/exportScope.ts` L8 の `isExportedFrame`（`src/export/__tests__/exportScope.test.ts` が `FRAME`／`SECTION` を true、`COMPONENT`／`RECTANGLE` を false と検証） | 実在。4.7.2 で名指しした関数はこれ |
| Starter チームは1ファイル3ページ、Drafts は無制限 | steering 未決10 の記載と出典（Figma ヘルプ「Starter plan overview」、2026-09-21 確認） | ステアリングの記載をそのまま採用。Web 再確認はしていない |
| Figma 公式が `file > page > frame` でのコンポーネント整理を推奨 | steering 未決10 の出典（Components, styles, and shared library best practices / Name and organize components、2026-09-21 確認） | 同上 |
| Figma 公式が String Variable の適用先に font-family を挙げる／W3C DTCG に `fontFamily` 型がある | steering「Variables の型と適用先（公式）」および未決4 の根拠 | ステアリングの記載を採用 |
| Text Style 8段階の根拠（実測 18 / 20px が6段階から外れた） | steering 未決5 と D-1 未決3 の実データ検証 | 一致 |
| 既存の記述（4.3.4 の基本姿勢・自動判定しない・促さない、4.7.4.1 の切り分け、4.7.4.3 の確かめる場所、4.9 の推測禁止） | `docs/design.md` の当該節 | 引用元の文言と一致することを確認して参照した |

### 自分で決めた点（コーディネーターの承認対象）

1. ~~**書体 Variable の判別方法**を「`scopes` に `FONT_FAMILY` ないし `ALL_SCOPES` を含むか」と設計書に書いた。~~ → **コーディネーターの指摘（第2ラウンド）で差し替え済み。** 新規 Variable の既定が `ALL_SCOPES` のため、テキストの中身用の STRING Variable まで書体として出る誤検知があった。「`FONT_FAMILY` を含み、かつ `ALL_SCOPES` を含まない」に修正し、Setup のスコープ設定への依存も 4.3.4 に明記した（コミット `ffc73b1`）。
2. **4.7.2 を「2つの出力」から「3つの出力」に拡張**し、付け忘れの知らせを (3) として 4.7.2 に定義した（4.7.4.3 は並びと置き場所だけを持つ）。原則B「1箇所にだけある」に寄せるため。
3. **グラデーション stop の未バインドを Review のチェック項目に1行足し**、「テーマ整合4種」を「5種」に更新した（ゲート表とチェック項目の両方）。発火表に行を足す以上、項目数の記述が古くなるため。
4. **4.5.2.1 の `gradientStops` に `colorToken?` を追加**した。4.3.4 が stop のトークン名を出すと定めているのにフィールド詳細に無く、未決7 で stop を error 対象にした結果この不一致が目立つため。
5. **4.8.1 の「入力データの読み方」に `screenshots-dark/` / `assets-dark/` を追加**した。zip に入れると決めたものが prompt.md の説明対象から漏れる状態を残さないため（レビュー #10 の Valid と同じ話だが、テンプレート本文の改訂は実装タスク側）。
6. **4.3.4 の「ツールは促さない」に例外1文**（既にあるトークンと同じ値なのに指していない箇所だけは知らせる）を足した。未決9 の結論と正面から読み合わせると矛盾に見えるため。
