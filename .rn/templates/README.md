# README template

> Rules（書くときにこの節は消す）
>
> - 読む人は初めての利用者。これだけ読んで使い始められる
> - 冒頭3行で「何・誰向け・どう始める」
> - 1見出し = 読み手の1つの問い。見出しは英単語
> - 図・表・箇条書きで見せる。文は3行まで続けない
> - 「何をするか」だけ。理由・経緯・履歴は書かない（理由は設計書、履歴は git）
> - 最新の姿だけ。名前・コマンド・値は実物から写す
> - 網羅しない。迷う所だけ書く。参照は設計書へのリンク1つ
> - 図はテキスト（Mermaid・ツリー・表）。画像は使わない

---

# {Name}

{一文: 何を、誰のために}

```mermaid
flowchart LR
  A[入力] --> B[{Name}] --> C[出力]
```

## Quick start

```
{入れる}
{動かす}
```

## How it works

```
{流れを1本の図で。手順の名前は画面のボタンと同じ}
```

## Usage

### {操作 1}

{何をすると、何が起きるか。2〜3行}

### {操作 2}

| 項目 | 意味 |
|---|---|
| {欄} | {何を入れるか} |

## Output

```
{出力の形をツリーで}
```

## Limits

- {扱わないもの}
- {扱えないときの挙動}

## Development

```
{build}
{test}
```

設計の意図: [`docs/design.md`](docs/design.md)
