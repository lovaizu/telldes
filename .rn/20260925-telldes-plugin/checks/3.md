# 3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 2つの文書が `.rn/templates/` の型（節の順と、各節のコメントにある Purpose / How）に沿っている。表と太字が無い | OK | README の見出しは冒頭の一文 → Who it is for → Getting started → Usage → Develop → License、設計書は Goals → Non-goals → Approach → Structure で、テンプレと同じ順。冒頭は日本語の一文だけ、Getting started は番号付きでプラグインを開くところまで、Who it is for はデザイナーの2つのストーリー、Usage はストーリーごとの番号付き手順で「渡らない」ものは効く手順の中に畳んである。Non-goals は作らない機能5つに理由付き、Approach は「選択: 理由」の箇条と Assumes、Structure は Mermaid のフローチャート1つと部品ごとの持ち物1行（zip と CC の箇条は README にあるので無い）。両文書を `\*\*|^\|` で grep して 0 件。テンプレの HTML コメントは写していない | OK | e5d86ec・b9874dd・9dce695 の3回で QA・文章・事実確認・設計の4人が各回レビュー。最終回（9dce695、c5c70b8 で2語直し）で節の順・表と太字の不在を grep と通読で確認 |
| README だけを読んで、初めての人がリポジトリの入手からプラグインを開き、Figma ファイルを用意し、Export して CC へ渡すまでを追える。書かれた画面・操作・出力が、steering の Acceptance criteria と実装済みの画面の言葉と一致し、まだ作っていない部分は設計で決まっていることだけを書いている | OK | Getting started は 1 bun → 2 git clone / bun install && bun run build → 3 ファイルを開いて Import plugin from manifest… → 4 Plugins → Development → Telldes。Usage の1つ目でページの用意（渡らないものは Not exported、Section の中は渡る）・Setup（トークンの定義、タブの印）・デザイン（Auto Layout 無し・同名・デフォルト名。Tokens タブに出るもの／Not exported になるもの）・Section（Frames タブに Section 名の行）・From width / Content width / Page title・note（プロパティパネルからも）・Dark support（Light か Base につなぐ）まで、2つ目で Review（error / notice の定義、持ち主に「トークンにつないでいない値（Tokens タブに並ぶ）」）→ error 4つ（Dark support ON で Light か Base につないでいない色、同名が Dark に無い色、Dark コレクションが無い、Web ページ名の重複）と notice 2つ（トークンと同じ値なのにつないでいないもの、ON では色だけ error に置き換わる／From width 無し）→ Export → zip の中身（渡らないレイヤーの note は README.md）→ CC に伝える文言まで。画面の言葉は `src/ui/` に照らした: Frames / Tokens（App.tsx）、Setup（TokenList.tsx）、Review と「● n errors ○ n notices」と絞り込み（TopBar.tsx）、Light \| Dark（TopBar.tsx）、Export settings・Dark support・Rules for every page・Page title・From width・Content width・Save（Detail.tsx）、Not exported（ScreenList.tsx）、✎（parts.tsx）。理由の語（ため・から・理由・なぜ・代償）を grep し、残る「から」は理由でない（作ってから・どの幅から・パネルからも・bun.sh から）。Section での組、Scope が Font family / Font style の String、一番狭いフレームの From width 空、note をプロパティパネルから開くこと、error「Light と同名の変数が Dark に無い色」は steering #3 と Acceptance criteria の決定どおりで、実装（`src/core/screens.ts` は Section をまだ読まず、`tokens.ts` は String を落とし、Detail.tsx に Same page as が残る）より先に文書に置いてある | OK | 最終回の4観点レビューで、Getting started の clone URL・manifest・scripts・dist パス、画面の言葉（Frames / Tokens、Setup、Review、● n errors ○ n notices、Export、Light \| Dark、Export settings、Dark support、Rules for every page、Page title、From width、Content width、Save、✎、Not exported）を `src/ui/` と照合。実装に先行する部分は steering #2・#6・#8・#9・#10 の手当てを確認。残っていた「同名が Dark に無い色」の Dark support ON の限定と持ち主の語は c5c70b8 で直した |
| 設計書の Goals が README のストーリーと1対1で、Approach の各選択に理由があり、Structure の矢印が Usage の手順と同じ名前。直した経緯が残っていない | OK | ストーリー1「CC が組めるかたちに整えられる」↔ Goal 1「スクリーンショットが Export の画像と一致」、ストーリー2「直すところと渡らないものを知り zip にして渡せる」↔ Goal 2「渡す前に、壊れるものと渡らないものが分かる」。Approach の14の箇条はすべて「選択: 理由」の形で、OOUI（機能ごとに分けると直す場所と知らせる場所がずれる、持ち主は1つの判断の対象、Dark コレクション無しはファイルの error 1つ、値の行、Export settings は1つの事実を1か所に）・英語（Community で言語が分からない）・Review（error の定義と壊れ方2例、知らせる規則と数値は Scope でも照らす理由、報告しない理由、自動実行の理由）・ダーク（複製しない理由、別コレクション＋同名の理由、付け替わらない色が error、既定 OFF、テーマをファイルに保存、代償）・note をパネルから開ける理由・Setup 無しでも出る理由・id で区別する理由が付いている。Structure の矢印は 読む・書く / 読んだデータ / 読む・選ぶ / Setup / note / Export settings / Light \| Dark / Export（画像） / Review / Export / Setup / Light \| Dark / errors / notices / Export / prompt.md で、Review・Export・Setup・Light \| Dark は Usage に同じ綴りで出る。実装（画面が Figma 側に読む・選ぶを頼み、判断は読んだデータと設定を受けて結果を返す）と同じ分け方。設計書に押す順や欄の場所の手順（3コレクションに分ける、Export 後に戻す、開いたら促す）は無く、回数・以前の形・変更の言及も無い | OK | Goal 1↔ストーリー1、Goal 2↔ストーリー2。Approach 14 項すべてに子の理由。矢印に Review / Export / Setup / Light \| Dark。分割前後で文の集合が同じことを文単位で確認。経緯語 0 件 |
| 2つの文書に互いに矛盾する記述が無く、古い記述（3つのタブ、Notes タブ、無い steering へのリンク、フレーム名での対応付け、zip の `steering.md`、Auto Layout 未適用を error にする、Same page as）が無い | OK | 両文書を `3つのタブ|Notes タブ|steering|フレーム名で|Auto Layout 未適用|Same page as|展開|Export 設定|違反` で grep して 0 件（「知らせ」の hit は動詞だけ）。通読で照らしたのは: 渡すフレームの範囲、Section 名が Web ページ名、トークンになるもの（Color・Number・Scope が Font family / Font style の String・Text Style・Effect Style。Text content の String・Boolean・Color Style は出さない）、From width の入れ方、error 4つ（Light か Base につないでいない色／同名が Dark に無い色／Dark コレクションが無い／Web ページ名の重複）と notice の規則（トークンと同じ値なのにつないでいないものは Dark support に依らず知らせ、ON では色だけ error に置き換わる。README 手順3と設計書の Review の項が同じ）、Light・Dark・Base と Dark に Light と同名の変数、Dark support の既定 OFF、Export 後に Light へ戻る、Setup の置き場所とタブの印、Export settings の持ち主（題名は Web ページ、1枚だけならそのフレーム）、渡らないものと note の行き先。両文書で同じ | OK | 古い記述 0 件。error 4つと notice の規則、渡す範囲、トークンの範囲、Light / Dark / Base、既定 OFF、Export settings の持ち主を両文書で照合して同じ |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 初めてのデザイナー／直す人になりきって通読し、各節をテンプレの Purpose に照らした。画面の言葉と steering の決定を実物に照合。3回の指摘（ページ直下と Section の矛盾、error 一覧のずれ、notice の条件、Structure と実装の食い違い）はすべて解消 |

## Expert Reviews (axes the task needs)

### Design Expert
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | 2文書の役割分担（README = 何をするか、設計書 = なぜそうか）が成り立ち、Approach の各決定が Goals / Non-goals から導ける。書き直し前の決定で理由なく落ちたものは戻した |
| System-wide integrity (interfaces, cross-doc consistency) | OK | Structure が `src/figma` / `src/ui` / `src/core` の分け方と一致。steering の Tasks・Acceptance criteria を設計書に合わせて直した（fad6e34・4e3bb11） |

### Craft Expert (writing)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 手順は「する → 見える」、語は初出で定義、error / notice に統一、長い項は決定の1行と理由の子に分割 |
| Consistency with existing style | OK | テンプレ `.rn/templates/` の型と画面の英語の綴りに一致。表・太字なし |

### Verification Expert (fact-check)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 約75件の主張を steering・`src/`・package.json・manifest.json・LICENSE・git remote に照合。Figma の事実（Starter の制約、Scope 名、メニュー経路）は知識で確認し、未確認は Assumes に置いた |
| Coverage (edge cases / claims / steps) | OK | Getting started から CC へ渡すまでの全手順、error / notice の全項目、推奨一式の名前と値、Structure の全矢印を確認 |

## Overall Verdict

- Self-check: OK（README 70 行・設計書 51 行。最後のレビュー反映後に両文書を上から通読し、テンプレの各節の Purpose に照らした。error 4つと notice の規則が両文書で同じこと（`error になるのは`・`Review は出力` を grep し照合、`Light の変数につないで`・`Dark support OFF のときに` は 0 件）、README に理由・設計書に手順が無いこと、Goals とストーリーの1対1、Structure の矢印に Review があり実装の分け方と Usage の手順名に対応すること、Mermaid のラベルが `"…"` で囲まれていること、表・太字・経緯・古い記述の不在を grep と通読で確認。出どころ（書き直し前の設計書 6977b3f・steering・`src/`・package.json・manifest.json・LICENSE）に無いことは書いていない。Section での組・書体の String・From width 空・プロパティパネルの note・Dark の同名変数の error は実装より先に文書に置いてあり、#2 の残 step と以降のタスクで追いつく）
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes
