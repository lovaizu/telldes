# 1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `bun install && bun run build` だけで `dist/` ができ、Figma デスクトップの Drafts で manifest から読み込むと画面が開く | OK（実機は未確認） | `dist` を消してから `bun install --frozen-lockfile && bun run build` → `dist/ui.html`（43.0 kB、`<script>` 1つをインライン、外部 src/link なし）と `dist/code.js`（13.3 kB、IIFE、import/require なし、`bunx acorn --ecma2017` で構文解析が通る）ができた。UI のビルド設定は Vite 8 の `rolldownOptions` に変え、ビルドに非推奨の警告は出ない。`manifest.json` は main/ui が dist を指す。Figma での読み込みはユーザー待ち | OK | ユーザーが Figma デスクトップの Drafts（Chee Design Portfolio）で manifest から読み込み、画面が開いた |
| 実機で、開いたファイルのトークン・画面・レイヤーが読まれ、画面側に届いている（件数などで確かめられる） | OK（実機は未確認） | UI と Figma 側のやりとりは `id` 付きの要求と、同じ `id` の `{ id, ok, result }` か `{ id, ok, error }` の返事に揃えた（UI は `request("read", {})` の Promise で受ける。両端の switch に `assertNever`）。ビルドした `code.js` を `figma` を真似たグローバルの下で動かし、`{id:7,type:"read"}` に `id:7, ok:true` で、ファイルとページの pluginData、constraints・relativeTransform・absoluteBoundingBox・rotation・strokeJoin の MIXED・strokeCap・layoutGrids・overflowDirection・reactions、テキストのノード単位の値（textStyleId の MIXED など）と段落読み取りの 25 項目が返ること、途中のレイヤーが例外を出すと `ok:false` で「レイヤー「Broken」(RECTANGLE, id 1:4): boom」と名前・種類・id 付きの1件の失敗になることを確認。ビルドした `ui.html` をヘッドレス Chrome で開いて返事を真似ると、成功で件数が、失敗で「読み込めませんでした: …」が出て、メッセージが空でも失敗表示になることを確認（一時ファイルは削除済み）。Figma 実物での読み取りはユーザー待ち | OK | 実機で読み取り成功: 変数コレクション1・変数4・Color Style 1、画面2（top 1440×8696 / 240 レイヤー、components 1440×2921 / 49 レイヤー）が画面に届いた。失敗時はレイヤー名付きで出ることを模擬で確認 |
| 依存は `solid-js` の v2 系と、Vite・TypeScript などその時点の最新版で、型検査が通る | OK | 依存は前回から変えていない（solid-js / @solidjs/web 2.0.0-rc.9、@solidjs/vite-plugin 3.0.0-next.44、vite 8.3.1、typescript 7.0.2、@figma/plugin-typings 1.139.0、vite-plugin-singlefile 2.3.3）。`bun run typecheck` は figma・ui・core・node（vite の設定ファイル）の4つの tsconfig を strict で通す。vite の設定ファイルに型の誤りをわざと入れると落ちることを確認（戻し済み） | OK | npm の dist-tags と照合して最新。型検査4つが通る（再レビューで確認） |
| Figma 側のコードが、画面の描画や判断を持っていない（読み取りと書き込みの窓口だけ） | OK | `src/figma/` は `main.ts`（要求を読み取りの窓口へ渡して返事を返すだけ）と `read.ts`（Figma の値を写すだけ）。`tsconfig.core.json`（core・shared、lib は ES のみで DOM も Plugin API のグローバルも無し）を足し、ui の設定は src/ui だけを対象にした。わざと `src/core` に `document` を書くと core の設定で「Cannot find name 'document'」、`figma` を書くと「Cannot find name 'figma'」で型検査が落ちることを確認（戻し済み）。`code.js` に DOM 参照なし | OK | main.ts は振り分けだけ、read.ts は写すだけ。画面か否かの判断は src/core。core は ES のみの設定で figma/DOM を使うと落ちる |

テストのカバレッジ: 対象外。テストは #10 まで書かない決まりのため。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 件数だけでは中身の正しさが分からない指摘を受け、読み取り失敗にレイヤー名を入れ、実機はユーザーの実際のファイルで確認した |

## Expert Reviews (axes the task needs)

### Design Expert
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | figma / shared / core / ui の分け方が design.md どおり。ページ直下はすべて読み、画面の判断は core |
| System-wide integrity (interfaces, cross-doc consistency) | OK | 指摘 M1〜M3（id 付きの要求と共通の返事、core の型分離、読み取り項目の追加）を e48c04a で直し再レビュー合格。SECTION など画面でないものの扱いは #2/#5、README の食い違いは #11 で扱う |

### Craft Expert (coding)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | OK | 非同期 API・mixed の扱い・Solid v2 の書き方が正しい。rolldownOptions・空メッセージ・event.data ガード・rotation の指摘は直し済み |
| Consistency with existing style | OK | 名前の付け方がそろい、コメントは理由だけ |

### Verification Expert (test)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | 空の clone からビルド、code.js の ES2017 構文解析、型検査の分離を注入で確認、模擬 figma で読み取り、実機で読み取り成功 |
| Coverage (edge cases / claims / steps) | OK | 空のページ・フレームの無いページ・入れ子のインスタンス・混ざったテキスト・非表示を追った。エラーのあるコンポーネントセットは #5 の手順に追加 |

## Overall Verdict

- Self-check: OK（ビルド・4つの型検査・core の分離・要求と返事の形・レイヤー名付きの失敗・UI 表示は確認済み。Figma 実機での読み込みと読み取りはユーザー待ち）
- QA: OK
- Design expert: OK
- Craft expert: OK
- Verification expert: OK
- Ready to check off: Yes（実機での読み込みと読み取りをユーザーが確認）
