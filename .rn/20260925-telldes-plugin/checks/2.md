# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Figma の中で、Setup・Review・note・Export（設定を含む）・Light / Dark のすべての操作を画面で試せ、それぞれの結果がどこに出るかが見える | OK（実機は未確認） | ビルドした `ui.html` を Figma と同じく iframe に入れたヘッドレス Chrome（680×580）で開き、仮の読み取りデータ（desktop と mobile の2フレーム・1フレームだけの about・非表示レイヤー・Figma のページ直下の RECTANGLE と COMPONENT_SET・トークンと同じ色／どのトークンとも違う色・Light / Dark / Base の変数）を返して全操作を押した。Setup → トークンの一覧に「Setup」印の行と「仮 #4」、Review → 上部の error / 知らせの件数と各行・詳細、note → 非表示レイヤーで書いて保存し「渡らないもの」の行に ✎、Export 設定 → ファイル（ダーク対応・共通ルール）、Web ページ（名前 = zip のフォルダ名・題名）、画面（入る Web ページ・切り替える幅・コンテンツ幅）。変えると Review が走り直し件数が変わる（切り替える幅を入れると知らせ 2→1、ダーク対応 OFF で error 3→0）。Export → 画面の一覧の見出しのボタンは error があっても押せ、押すと Review し直して「error が 3 件残っています…（トークンのタブ 2 件・画面のタブ 1 件）」で止まり、0 件なら「Web ページ 2 つ（画面 3 つ）をフォルダごとに」と出る。Review か Export の結果が出ているときに設定を変えると、結果行は古い件数を残さず「設定が変わったので Review し直しました: error n 件」に替わる（修正2回目で確認: ファイルの詳細で Export が error 2 件で止まった後にダーク対応 OFF → 上部 error 0・結果行「error 0 件」の通常色、ON に戻す → 上部 error 2・結果行「error 2 件」の赤、画面の詳細で Export が止まった後に切り替える幅を入れる → 上部と結果行がともに error 2）。画像 → 画面の詳細に「渡す画像 写真 1・アイコン 1 仮 #8」。Light / Dark → 上部の切り替え、ダーク対応 OFF では Dark が淡色になり、押すと理由が下の結果行に出て切り替わらない。実機での操作はユーザー待ち |  |  |
| 画面は機能ごとの区画（Review の欄、note の欄など）を持たず、オブジェクトの一覧と詳細でできている。違反・知らせ・note は持ち主の行と詳細に出る | OK | 左は「画面」「トークン」の一覧（どちらのタブの件数も渡すものの数）、右は開いたオブジェクト（ファイル・Web ページ・画面・レイヤー・トークン・トークンにしていない値）の詳細、上部はファイル全体の操作、下は結果の1行だけ。画面の一覧は Web ページ → 画面（幅ごと）→ レイヤーの木。持ち主は1つ: トークンと同じ色（#FFFFFF = bg）は bg の1行だけ（「bg につなぐ」）、どのトークンとも違う色は使っている数によらず「トークンにしていない値」の行（修正2回目で、1か所だけの #FF0000 も値の行に出て ● 1 が付くことを確認。design.md と同じ1つの決まり）、「切り替える幅が無い」は2画面以上の Web ページの画面だけ、Dark のコレクションが無いのにダーク対応 ON は上部のファイルの行と詳細。渡らないものの行にも ✎ と ●○ が付く。たたんだ Web ページの行にも中にあることを「…」で出す |  |  |
| 操作しても Figma のファイルが変わらない。仮の値は仮と分かる形でコードにまとまっていて、各機能のタスクで差し替える場所が決まっている | OK | `src/figma` は今回変えていない（書き込みは `select.ts` の選択とスクロールだけのまま）。ハーネスで全操作を押しても Figma 側へ行った要求は `read` と `select` だけ。仮の値はすべて `src/ui/placeholders.ts` にあり、節ごとに差し替えるタスク（#4 Setup・#5 Review・#6 note・#7 Export 設定と Export・#8 画像・#9 Light / Dark）を書いた。`Tentative` は `TASK` のキーを受け、番号と機能名は `TASK` の1か所にだけある（画面の「仮 #n」の title に「#8 画像・アセットの書き出し で本物に差し替えます」などと出ることを確認）。Web ページの組み分けなどの設定はメモリにだけ持つ（#7 で保存に差し替え） |  |  |
| 実物のデータのトークン・画面・レイヤーが一覧に並び、レイヤーを選ぶと Figma でもそのレイヤーが選ばれる | OK（実機は未確認） | 一覧と詳細は #1 の読み取りデータ（`FileData`）そのものから出す。Web ページの組み分けは core の `groupWebPages`（宣言が無ければ画面ごとに1つ、名前からは推し量らない）で、top-mobile の Web ページを top-desktop に変えると1つの Web ページの下に2画面が並び、名前を top にすると一覧・詳細・zip の中 `top/` が変わることを確認。行・「Figma で選ぶ」・「使っている所」を押すとハーネスに `select` とその layerId が届き、見つからない返事では結果行に error が出て、次に選べたときや別のオブジェクトを開いたときに消えることを確認。実機での選択はユーザー待ち |  |  |

テストのカバレッジ: 対象外。テストは #10 まで書かない決まりのため。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") |  |  |

## Expert Reviews (axes the task needs)

### Design Expert
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits |  |  |
| System-wide integrity (interfaces, cross-doc consistency) |  |  |

### Craft Expert (coding)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice |  |  |
| Consistency with existing style |  |  |

### Verification Expert (test)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) |  |  |
| Coverage (edge cases / claims / steps) |  |  |

## Overall Verdict

- Self-check: OK（ビルド・4つの型検査・ヘッドレスでの全操作の通し（Dark のコレクションがあるファイルと無いファイルの2通り）・書き込みが選択だけであることは確認済み。修正2回目: 設定を変えた後に結果行が上部の件数と食い違わないこと、1か所だけのトークンに無い色が値の行に出ることをヘッドレスで確認。design.md の Dark の節の「ページ」を「Figma のページ」「Web ページ」に分けた。Figma 実機で Web ページの組み分けなどを触って分かりにくいところを直す手順はユーザー待ち）
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- Ready to check off:
