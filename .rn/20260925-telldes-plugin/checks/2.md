# 2 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Figma の中で、Setup・Review・note・Export（設定を含む）・Light / Dark のすべての操作を画面で試せ、それぞれの結果がどこに出るかが見える | OK（実機は未確認） | 修正3回目（英語化・Figma 風の見た目・Export を上部へ・件数で絞り込み）: ビルドした `ui.html` を iframe に入れたヘッドレス Chrome（680×580）で、読み取りの返事を真似たハーネスから各操作を押し、画面と結果行を撮って確かめた。Setup → Tokens に「Setup」の行が並び、結果行「Setup would add 55 tokens…」。Review → 上部の「● 3 errors」「○ n notices」と各行の ●○、詳細の「Errors and notices」。上部の件数を押すと一覧が error（または notice）を持つ行だけになり、上に「Only rows with errors / Show all」が出て、Show all か件数をもう一度押すと戻る。今のタブに無ければ持つほうのタブに切り替わり、残りが 0 になれば絞り込みは自動で外れる。Export（上部）→ error 3（Dark のコレクションが無いファイルでは 4）で止まり、結果行「Export stopped: 3 errors left…」と同時に error の行だけに絞り込む。Dark support を OFF にして error 0 で押すと「Export would write telldes-export.zip: 3 pages…」（組にした後は「2 pages (3 frames)…」）。note → Title レイヤーで書いて Save、行に ✎。Export 設定 → ファイル（Dark support・Rules for every page）、フレーム2枚以上の Web ページ（Name・Page title）、フレーム（Same page as・1枚だけのページなら Page title・From width・Content width）。mobile の Same page as を desktop にし From width を入れると notice が 1 件になり、notice の絞り込みで mobile だけが残る。Light / Dark（上部左、ファイル名の隣）→ 切り替わりと結果行、Dark support OFF では Dark が淡色で押すと理由が出る。修正5回目: 上部右は「● n errors ○ n notices ⟳ Export」だけにし、⟳（Review again）で Review が走り結果行が出る。Setup は Tokens の一覧の頭に移し、足りないトークンがあれば青で「55 tokens missing」を添え、無ければ控えめな見た目。Figma の dark の色変数をハーネスで入れた表示でも読めることを確認。実機はユーザー待ち 修正7回目: ⟳ を名前付きの「Review」ボタンにして件数の前に置き（押すと Review が走り結果行が出る）、足りないトークンがあるとき Tokens タブに「+55」の印（hover「Setup has 55 tokens to add」）を出す。足りなければ出ず（全部そろったファイル）、Setup の動作は変わらない。修正8回目: フレーム1枚だけの Web ページは Web ページの行を出さずフレームの行だけにし、見出しは組が無ければ「2 frames」、あれば「3 frames in 2 Web pages」。ユーザーのファイルを真似た2枚（top 1440・top-sp 375）で top/top の2段が消え、Same page as で組むと「top · 2 frames」の行の下に2枚が並び、None に戻すと元に戻ることを確認。 |  |  |
| 画面は機能ごとの区画（Review の欄、note の欄など）を持たず、オブジェクトの一覧と詳細でできている。違反・知らせ・note は持ち主の行と詳細に出る | OK | 構成は変えていない: 左は Screens / Tokens の一覧、右は開いたオブジェクトの詳細、上部はファイル全体の状態と操作（Light / Dark・Review ボタンとその件数・Export）、Setup は Tokens の一覧の頭（足りないトークンがあれば Tokens タブに「+n」の印）、下は結果の1行。Review の件数は別の一覧を作らず、既存の一覧を持ち主の行に絞るだけ。error・notice・note は持ち主の行（●○✎）と詳細に出る（#FFFFFF は color/bg の行、どのトークンとも違う #FF0000・#0000FF は「Values without a token」の行、Dark のコレクション無しはファイルの行と詳細） |  |  |
| 操作しても Figma のファイルが変わらない。仮の値は仮と分かる形でコードにまとまっていて、各機能のタスクで差し替える場所が決まっている | OK | `src/figma` の変更はエラー文の英語化（`read.ts` の1行）だけで、書き込みは `select.ts` の選択とスクロールのまま。ハーネスで全操作を押しても届いた要求は `read` と `select` だけ。仮の値は `src/ui/placeholders.ts` に集めたまま、節ごとに差し替えるタスク（#4〜#9）を書いてある。画面では「仮 #n」の印をやめ、下の結果行の右に1つ「Prototype — results are placeholders」を出し、その hover に各タスクの一覧、仮の値（`Tentative`）の hover に「Placeholder. Task #7 (Export settings) replaces it.」などが出ることをハーネスで title を読んで確認 |  |  |
| 実物のデータのトークン・画面・レイヤーが一覧に並び、レイヤーを選ぶと Figma でもそのレイヤーが選ばれる | OK（実機は未確認） | 一覧と詳細は読み取りデータ（`FileData`）から出す。ハーネスで画面・レイヤーの行、詳細の「Select in Figma」、「Used in」のリンクを押すと `select` とその layerId（1:1・2:1・1:3・1:2）が届く。見つからない返事では結果行に赤で「This layer is no longer in Figma…」が出る。実機での選択はユーザー待ち |  |  |

テストのカバレッジ: 対象外。テストは #10 まで書かない決まりのため。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") |  |  |

## Expert Reviews (axes the task needs)

### Design Expert
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits |  |  |
| System-wide integrity (interfaces, cross-doc consistency) |  |  |

### Craft Expert (coding)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice |  |  |
| Consistency with existing style |  |  |

### Verification Expert (test)
| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) |  |  |
| Coverage (edge cases / claims / steps) |  |  |

## Overall Verdict

- Self-check: OK（ビルド・4つの型検査・ヘッドレスでの全操作の通し（Dark のコレクションがあるファイルと無いファイルの2通り）・書き込みが選択だけであることは確認済み。修正2回目: 設定を変えた後に結果行が上部の件数と食い違わないこと、1か所だけのトークンに無い色が値の行に出ることをヘッドレスで確認。design.md の Dark の節の「ページ」を「Figma のページ」「Web ページ」に分けた。Figma 実機で Web ページの組み分けなどを触って分かりにくいところを直す手順はユーザー待ち。修正3回目: UI の言葉をすべて英語にし（`src/figma/read.ts` のエラー文も）、Figma の色変数と 11px・24px の行に合わせ、常に出ていた説明文は hover に移すか消した。「仮 #n」は結果行の「Prototype — results are placeholders」1つにまとめ、タスクは hover で出す。Export を上部に移し、止まったら error の行に絞る。上部の件数で一覧を error / notice の行に絞れ、Show all で戻る。ビルドと4つの型検査が通り、ヘッドレスで light / Figma dark の両方と上の全操作を撮って確認。design.md の OOUI の節に、Export を上部に置く理由と UI を英語にする理由を足した。実機での確認はユーザー待ち。修正4回目: Light / Dark がプラグイン自体のテーマ切り替えに見えないよう、右端から左のファイル名の隣に移し「Canvas」と名前を付け、hover で「このページの変数を切り替える。プラグインは Figma のテーマに従う」と出す。ヘッドレスの 680×580 で light / Figma dark とも上部が1行に収まり、切り替えと Dark support OFF での Dark の淡色・理由表示が前と同じに動くことを確認。修正5回目: 上部から Setup・Review のボタンと「Canvas」の文字を外し、右は件数・⟳（Review again）・Export だけにした。Setup は Tokens の一覧の頭に置き、足りないトークンがあれば青く件数を添える。ヘッドレスの 680×580（light / Figma dark）で上部が1行に収まり、⟳ で Review が走ること、件数での絞り込み、Export が error で止まり 0 なら通ること、Setup の目立つ／控えめの切り替えと動作、Light / Dark と Dark の淡色・理由表示を確認。design.md の OOUI の節の置き場所の理由を書き直した。修正6回目: UI の言葉で「screen」を Figma の言い方「frame」に改めた（タブ Frames、種別 Frame、件数、Export 設定の hover、仮の指摘文、Export の結果行）。コードの名前は変えていない。ビルドと4つの型検査が通り、ヘッドレスで Frames タブ・frame の詳細と Export 設定・Export が止まる／通る結果行を撮り、表示文と title・aria-label に「screen」が残らないことを確認。修正7回目: 上部の ⟳ を「Review」と名前の付いたボタンにして件数の前へ移し、足りないトークンがあるとき Tokens タブに「+n」の印を出した（error・notice の印とは別の形）。ビルドと4つの型検査が通り、ヘッドレスの 680×580（light / Figma dark）で上部が1行に収まり、Review ボタンで Review が走ること、Frames タブを開いたままで印が出て全部そろったファイルでは消えること、Setup が今までどおり動くことを確認。design.md の OOUI の節を合わせた。修正8回目: フレーム1枚だけの Web ページはフレームの行として出し、題名はそのフレームの Export 設定（Page title）で入れる。組み分けの欄は「Same page as」（None か、ほかのフレーム／組）にした。ビルドと4つの型検査が通り、ヘッドレスの 680×580（light / Figma dark）で、組む・外す（先頭のフレームを外しても残りは組のまま）、見出しと件数、From width の notice、絞り込み、Export の止まる／通る結果行を確認。design.md の OOUI の節に理由を1行足した）
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- Ready to check off:
