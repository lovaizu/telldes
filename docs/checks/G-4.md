# G-4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `README.md` に除外物が明記されること | OK | `src/App.tsx`'s `handleExportData` `readmeContent` array now has a `## Not included in this export` section with 2 bullets: (1) `prompt.md`/`steering.md`/`README.md` are generated from templates rather than derived from design nodes (only `spec.json`/`screenshots/`/`assets/` map 1:1 to Figma nodes) — this is the "App.tsx 等の非デザインノード由来ファイル" category from the task description, generalized to the zip's own non-node-derived files. (2) Component/Component Set definitions placed directly on the page (outside any frame) are excluded and flagged as a suggestion during Review — matching `checkBareRootComponents` in `src/checks/scopeChecks.ts` (G-3) and the export-scope policy in `docs/telldes-design.md` 4.7.4 (line ~605: bare root Component/Component Set definitions are excluded from export, announced via Review 4.7.2). | PASS | Independent review confirmed both required exclusion categories are present and factually match the design doc/G-3 implementation, with no inaccuracies. |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Content accuracy | PASS | Both exclusion categories (template-generated meta files, bare root Components) verified against `docs/telldes-design.md` 4.7.4 and `src/checks/scopeChecks.ts`'s `checkBareRootComponents` — no factual gaps. |
| Build/test verification | PASS | `bun run build` succeeds (UI + code bundles, 0 errors). No new tests added — `src/App.tsx` has no existing test file or established test pattern for its internal helpers (`generateSectionTasks`, `resolveFrameFolderNames`, etc. are likewise untested), so this string-content addition follows existing project practice. `bun run test` remains 150/150 passing (unaffected, App.tsx isn't part of the test suite). |

## Expert Reviews (code changes only)

### Language Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Wording clarity | PASS | Designer-readable English, no typos/grammar issues. "Review" correctly capitalized as a reference to the plugin's actual Review tab. |
| Style consistency | PASS (minor note) | The two new bullets are prose sentences about categories rather than the `- \`file\` — description` pattern used in the "Contents" list above; a reasonable, readable deviation given the content differs in kind (exclusion rationale vs. file listing) — not a defect. |

### Software-engineering Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Scope | PASS | Diff limited to the `readmeContent` array inside `handleExportData`; no unrelated changes. |
| Correctness (found & fixed during review) | PASS | Review surfaced a real pre-existing bug: `.filter(Boolean)` strips all falsy values, including the `""` blank-line separators (not just the conditionally-`null` `tokens.json` entry) — so the rendered README had no blank lines between any sections, including before the newly added heading. Fixed by changing to `.filter((line): line is string => line !== null)`, which drops only the `null` sentinel and preserves blank-line strings. Verified with a standalone Node repro that the README now renders with correct blank-line spacing between all sections. `bun run build` re-confirmed clean after the fix. |

## Overall Verdict

- Self-check: OK
- QA: PASS
- Language expert: PASS
- Software-engineering expert: PASS (1 pre-existing bug found and fixed as part of this task)
- Ready for user review: Yes — per the project's standing autonomous-approval instruction, the user reviews all tasks together on the PR rather than per-task.
