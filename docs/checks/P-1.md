# P-1 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| prompt.md が設計書 4.8.1 節の7構成を全て含むこと | OK | 1.概要、2.入力データの読み方、3.最初にやること、4.コーディング手順、5.HTML導出ルール、6.Auto Layout→CSS対応表、7.完了時チェック の7セクション |
| HTML 導出ルールが設計書 4.3.6 節の全ルールを網羅すること | OK | header→`<header>`, footer→`<footer>`, nav→`<nav>`, section→`<section>`, heading→h1/h2/h3, text→`<p>`, cta/button→`<button>`, note link→`<a>` 全パターン |
| Auto Layout → CSS 対応表が設計書 4.6 節の全行を含むこと | OK | direction, wrap, gap, padding, primaryAxisAlign(4値), counterAxisAlign(3値), sizing(width/height × HUG/FILL/FIXED) |

## 総合判定

- セルフチェック: OK
- ユーザーレビュー可否: 可
