# U-1 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `CheckLevel` 型が `"error"` のみであり、`level: "suggestion"` を生成するコードがリポジトリに存在しないこと | OK | `src/checks/types.ts` は `export type CheckLevel = "error";`（`CheckResult.level` も維持。レビュー指摘Cにより「意図的な一段のガード」であることをコメントで明記）。`grep -rn '"suggestion"' src` のヒットは2件で、いずれもテスト ― `readmeBuilder.test.ts` の「README に "suggestion" が現れないこと」を検証するアサーションと、`code.test.ts` が「非 error レベルはブロックしない」を検証するために意図的に構築するテストダブル（`as unknown as CheckResult` でキャストした `level: "suggestion"` の偽結果）。プロダクションコードで `level: "suggestion"` を生成する箇所はゼロ。`CheckResult.suggestion`（改善方法テキスト）は残存 |  |  |
| `src/checks/variableChecks.ts` とそのテストファイルが存在しないこと | OK | `git rm` 済み。`ls src/checks/` → `__tests__ sizingChecks.ts structureChecks.ts traversal.ts types.ts`（今回さらに `scopeChecks.ts` を `src/export/exclusions.ts` へ `git mv`）。`grep -rn variableChecks src` ヒットなし |  |  |
| Review タブの描画結果に Suggestions セクションが含まれないこと | OK | `src/App.tsx` の Review パネルは Errors ブロックのみ。`grep 'Suggestions\|suggestion-label\|suggestion-item'` ヒットなし。Review パネルは `<Show when={hasRun()}>` → `<Show when={results().length > 0} fallback={All checks passed}>` → `<Show when={errors().length > 0}>` の3段。第2ラウンドで外側ゲートを `results()` 基準へ戻した（`errors()` 単独ゲートだと、将来 non-blocking level が再導入された日に「非 error の結果が存在するのに All checks passed」を描画してしまい 4.3.4 の黙殺になる）。Suggestions セクションは依然として存在しない |  |  |
| 設計書 4.7.2 節に「Variablesチェック（提案）」節が存在せず、源泉・範囲の4項目が Export README への出力として記載されていること | OK | 4.7.2 に「書き出し時の除外物告知（README出力）」節＋4項目（Color Style／STRING・BOOLEAN Variable／裸の Component・Component Set／typography トークン名衝突）。「除外物告知の走査範囲」ブロックを第2ラウンドで改訂: (1) インスタンス内部**も**走査する（主コンポーネントを別ページに置く定石構成で Color Style 使用ゼロと誤報告していたため。理由を明記）、(2) ノイズはレイヤー単位でなく「除外される物」単位（Color Style ごと／Variable ごと）への集約で抑える、(3) レイヤーパスの先頭セグメントは当該画面フレームの **zipフォルダ名そのもの**であり、フォルダ名とREADMEパスは同一関数で生成する旨を明記（旧記述の「4.5.2の一意性規約と同じ規則」は 4.5.2 がトップレベルフレーム間のフォルダ名規約であることと食い違っていた）。4項目の記述も Color Style／Variable ごと1件・使用レイヤー数・代表パス最大3件・`figma.mixed` の扱いへ更新。4.5 の zip ツリー行も `README.md ← zip内容の説明＋今回の書き出しの除外物（4.7.2）` のまま 4.7.4 出力物8と整合。**第3ラウンド**: (1) 4.7.2 の冒頭文を書き換え、本節が「Review（違反＋改善方法）」と「書き出し時の除外物告知（違反ではなく改善方法も持たない事実記録）」の2つの出力を定めることを明示（旧文は前半しかスコープしていなかった）、(2) 4.7.2 が委譲する先の 4.5.2 に「フォルダ名（ページ直下のセグメント）の規約」を新設し、パス区切り文字の無害化・`.trim()`・空名の `frame` フォールバック・`-N` 付与・**ページ直下の兄弟をフレーム優先の単一パスで命名する**規則を明文化、(3) 4.7.2 の先頭セグメント記述をその規約への委譲に書き換え、(4) Review 実行失敗時の通知（4.7.2）と README Contents 節が実際にzipへ書き込まれたものだけを列挙する旨（4.7.4）を追記 |  |  |
| 書き出した `README.md` の「Not included in this export」セクションに、実際に検出された項目のみが対象レイヤーパス（衝突はトークン名）付きで列挙されていること | OK | 検出は `collectExclusions({pageRootNodes, variables, textStyles}) → ExclusionReport`（`src/export/exclusions.ts`）。第2ラウンドで入力を `pageRootNodes` 1本に畳み、書き出し対象フレームは共有述語 `isExportedFrame` で内部導出（2つのノード集合が食い違う余地を構造的に消した）。`colorStyles` は `ColorStyleUsage{styleName, examplePaths(最大3), layerCount}`、`stringBooleanVariables` は `StringBooleanVariableUsage{variableName, examplePaths, layerCount}` に再編 ― 代表パスが「対象レイヤーパス」を満たす。レイヤーパスの先頭セグメントは `resolvePageRootSegmentNames`（`src/export/layerPath.ts`）― **第3ラウンド**でフレーム／非フレームの2系統・2集合だった命名を単一パスへ統合し、フレームを先に命名して接尾辞なしの名前を保持させた（`resolveFrameFolderNames` とバイト一致することを `layerPath.test.ts` で固定）。これにより `[COMPONENT "Home", FRAME "Home"]` で「フォルダ `Home/` が存在するのに README が `Home` は未書き出しと述べる」、`[FRAME "A/B", COMPONENT "A-B"]` で「同一文字列が2つの異なる物を指す」という自己矛盾が消えた。以降のセグメントは `uniqueChildName`。`ColorStyleUsage.styleName` は `string | null` で、`figma.mixed` 由来のエントリは `null`（表示文言は README 層 `readmeBuilder.ts` が持つ）。README 生成は純関数 `buildReadme()`。テスト: `readmeBuilder.test.ts`（行書式・単複・「e.g.」分岐）、`exclusions.test.ts` の end-to-end とフォルダ名一致3件、`zipBuilder.test.ts`（実際に組み立てた zip の `README.md` 本文に除外物行が載ること、スクリーンショット／アセットが実バイトでフォルダへ入ること、Contents 行が実際の中身を反映すること、先頭フレームのビューポート、全フレーム横断のセクションタスク、セクション皆無時のフォールバック）、`code.test.ts` の `export-data.exclusions` 実値検証。第3ラウンド追加: ページ直下命名3件（フレーム優先／サニタイズ後の衝突／同名裸 Component 2件が別エントリ）、`layerCount` がレンダリング文字列でなくレイヤーを数えること（レイヤー名が ` > ` を含む場合）、トークン名衝突の dedupe、`typography/` が先頭セグメントであること |  |  |
| 検出ゼロの項目については `README.md` に該当行が出力されないこと | OK | `exclusionBullet()` が `items.length === 0` で空配列を返すため見出し行ごと非出力。テスト "omits every exclusion category when nothing was detected" と "renders several categories at once and keeps the section last"（未検出カテゴリのみ非出力）で検証。`hasTokens:false` の tokens.json アサーションは Contents 節にスコープし直し、衝突文言との誤検知を排除 |  |  |
| 新たな問題が持ち込まれていないこと: (a) 残る既存テストが全グリーン | OK | `bun run test` → Test Files 12 passed (12) / Tests 217 passed (217)（第3ラウンド前 194、第2ラウンド前 159）。`bun run build` も UI（`dist/ui.html` 151.42 kB）・plugin code（`dist/code.js` 18.12 kB）の両ビルド成功。`npx tsc --noEmit` は 77→80 件（App.tsx 67→70、それ以外は 10 件で第1ラウンド以来まったく同一: specBuilder 6 / sizingChecks 2 / traversal 1 / ui.tsx 1）。増えた3件はいずれも `check-error` 表示用に足した `<Show>` ブロック由来の既存同種 `TS7026 JSX element implicitly has type 'any'`（solid-js の JSX 型基盤が未解決という既存問題）で、新規の型不整合ではない。むしろ `code.ts` の `frames` はインラインの構造型＋`as ExportDataMessage["frames"]` キャストをやめて `ExportFrame[]` を直接使う形にし、型の言い直しとキャストを1つ減らした |  |  |
| 新たな問題が持ち込まれていないこと: (b) error レベルのチェック（構造4種・サイジング1種）の検出挙動と改善方法メッセージが変わっていないこと | OK | `src/checks/structureChecks.ts` と `src/checks/sizingChecks.ts` は今回も無変更（`git status` に現れない）。既存テスト `structureChecks.test.ts`（19件）・`sizingChecks.test.ts`（8件）は改変せず全パス。`runAllChecks` は `runStructureChecks` + `checkSizing` の合成のまま、引数も `collectAllNodes(figma.currentPage)` で同一 |  |  |
| 新たな問題が持ち込まれていないこと: (c) Export のブロック条件（error 1件以上で中止）が変わっていないこと | OK | `src/code.ts` の run-export は `runAllChecks(collectAllNodes(page)).filter((r) => r.level === "error")` の件数でブロック（レビュー指摘Cにより明示フィルタを復活。CheckLevel が再拡張されても新レベルがブロッカーに昇格しない）。メッセージは `${errors.length} error(s) must be fixed before export` のまま。除外物レポートはゲート通過後に算出し `export-data` に同梱するのみ。`src/__tests__/code.test.ts` が (1) **trap なし**のエラーありページで `export-error` が出て `export-data` が出ないこと（第2ラウンドで分離。trap 同居版はゲートの `return;` を削っても trap 由来の `export-error` で両アサーションが成立してしまい、ゲートを検証できていなかった）、(2) trap ノードで除外物スキャンがゲート後に走ること、(3) サイジングエラー単独でもブロックされること、(4) クリーンなページで `export-data` がちょうど1回、実値の `exclusions` を伴って出ること、(5) `level` が error でない結果はブロックしないこと、(6) 書き出し経路の throw が `export-error` として UI に届くこと、(7) 書き出し単位が `page.children` であって部分木全体ではないこと（入れ子フレームがトップレベルフォルダにならない）、(8) Variables API／Text Styles API が throw しても `export-error` ではなく `export-data` が出ること、を検証。加えて第3ラウンドで `run-checks` にも try/catch を追加し（`check-error` を post、`src/messages.ts` に型を追加、`src/App.tsx` が `running` を解除して表示）、Review タブが「Running...」のまま固まらないことを `code.test.ts` で検証。これは U-1 の完了条件の外にある既存の欠陥であり、意図的に含めた ― ブロック条件そのものは変更していない |  |  |

### 検証手法の適用状況（Method）

- テストファースト: 第3ラウンドの各修正（ページ直下の単一パス命名、`layerCount` の node id キー化、Contents 行の実態反映、`run-checks` のエラー通知）も、先に失敗するテストを書いてから実装した。第2ラウンドの各修正（フォルダ名とREADMEパスの一致、除外物単位への再集約とインスタンス内部の再走査、ゲートテストの分離、走査入力の畳み込み、zip 組み立ての抽出）はいずれも先に失敗するテストを書いてから実装した。
- カバレッジ計測: カバレッジプロバイダ（`@vitest/coverage-v8` 等）は未導入で、依存追加は禁止のため `--coverage` は未実行。代わりに (a) テストが通る経路の列挙と (b) 変異テスト（手動）で代替する。
  - 変異テスト（第3ラウンド追加分）: 新たに11件の変異を注入して `bun run test` を実行し、**11件すべてが検知された**。内訳 ― ページ直下命名を旧来のフレームのみ2系統に戻す／`topFrames` を `collectAllNodes(page)` に差し替え／`fetchVariablesAndTextStyles` の try/catch を両方撤去／主ビューポートを先頭フレームから末尾フレームへ／セクション収集を先頭フレームのみに／セクション皆無時フォールバック文言の撤去／`addFilesToFolder` の呼び出し削除／Contents 行が常に screenshots・assets を約束する／`findBareRootComponents` のパスを `node.name` に／`UsageGrouper` の重複判定キーを node id からパス文字列へ／`typography` prefix 判定を `includes` へ。最後の1件は当初のテスト（`brand/typography/heading-md` × Text Style `heading-md`）では**生き残った** ― `includes` が通っても `parts.slice(1)` 由来のサブパスが `typography/heading-md` になり Text Style 名と一致しないため。`brand/typography` × Text Style `typography` というケースを足して初めて検知できるようになった（テストを追加してから再度変異を注入し確認）。
  - 変異テスト（第2ラウンド）: 16件の変異をソースに注入して `bun run test` を実行し、**16件すべてが検知された**。内訳 ― ゲートの `return;` 削除／`pageRootNodes` を `topFrames` に差し替え／`isExportedFrame` から `SECTION` を削除／`runAllChecks` から `checkSizing` を削除／`run-checks` が常に `[]` を返す／`level === "error"` フィルタ削除／`zipBuilder` が空の除外物レポートを決め打ち／`zipBuilder` が `hasTokens: true` を決め打ち／`segmentName` の `index < 0` フォールバック削除／`!rest` ガード削除／レイヤーパス先頭をフォルダ名でなく兄弟インデックスで命名／Variable のグルーピングキーをパスのみに変更／インスタンス内部スキップの再導入／代表パス上限の撤廃／Color Style 名の生 id フォールバック撤廃／README の「e.g.」分岐の潰し。
  - テスト済み: `findColorStyleUsage`（文字列 styleId / `figma.mixed` の専用バケット / 空文字 / フィールド欠如 / ネストパス / 同名兄弟の `-N` 区別 / parent なし / PAGE に到達しない連鎖 / 親の `children` に含まれないノード / スタイル名解決・null フォールバック・throw フォールバック / 多数レイヤーの集約と上限3 / 別スタイルは別エントリ）、`findStringBooleanVariableUsage`（STRING / BOOLEAN / 配列フィールド / COLOR・FLOAT 除外 / boundVariables なし / `getVariableById` の throw / 同 null / 1ノード複数フィールドの同一 Variable / 1ノードに異なる2つの Variable / 多数レイヤーの集約と上限3）、`findBareRootComponents`（COMPONENT / COMPONENT_SET / フレーム内は非対象 / 非 Component は非対象）、`findTypographyTokenCollisions`（衝突あり / STRING・BOOLEAN 除外 / prefix 不一致 / 同名 Text Style なし / サブパスなし / `typography/` と空名 Text Style / 空入力）、`isExportedFrame`、`collectExclusions`（4カテゴリ集約 / 全カテゴリ空 / フレーム自身も走査 / ページ直下 SECTION / ページ直下の裸ノードは非対象 / インスタンス内部を走査し集約すること（Color Style・STRING Variable 双方）/ 裸 Component 内部の二重計上なし / 二重到達の計上なし / フォルダ名一致3件）、`resolveFrameFolderNames`（素通し / 重複 `-N` / パス区切り無害化 / 空名）、`buildReadme`（tokens.json 行の有無＝Contents 節スコープ / frameNames 空 / テンプレート説明行 / 4カテゴリ個別 / 単複 / 「e.g.」分岐 / 全カテゴリ空 / 複数同時 / 4カテゴリ同時と順序）、`buildExportZip`（出力ファイル一覧 / 実レポートの README 反映 / tokens なし / フォルダ名とREADMEの一致 / レポート欠落時に throw / テンプレート差し込み）、`collectExclusions → buildReadme` の end-to-end、`src/code.ts` の `run-export`（エラーゲート・ゲート順序・サイジングエラー・走査スコープ・SECTION・非 error レベル・`export-data` の除外物・throw の握り）と `run-checks`（除外物カテゴリが混ざらないこと・実エラーの message/suggestion）。
  - 未テスト（自動テストなし）: `src/App.tsx` の Solid コンポーネント描画（`<Show>` ゲート、`level === "error"` フィルタの UI 側の双子、`check-error` 表示と `running` 解除）― DOM 環境と Solid のテストユーティリティが未導入で、依存追加は禁止のため。zip 組み立ては `src/export/zipBuilder.ts` へ抽出済みで、App.tsx 側に残るのはダウンロード処理のみ。ほかに `src/code.ts` の `select-node` / `save-note` ブランチ、レイヤーパスの DOCUMENT 直下という到達不能分岐。`fetchVariablesAndTextStyles` の API 欠如フォールバックは第3ラウンドで `code.test.ts` に追加済み（両APIそれぞれ）。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") |  |  |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits |  |  |
| System-wide integrity (interfaces, cross-doc consistency) |  |  |

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice |  |  |
| Consistency with existing style |  |  |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) |  |  |
| Coverage (edge cases / claims / steps) |  |  |

## Overall Verdict

- Self-check: OK（第3ラウンド。全9完了条件を再確認。`bun run test` 217 passed / `bun run build` 成功）
- QA:
- Design expert:
- Craft expert:
- Verification expert:
- Ready to check off:
