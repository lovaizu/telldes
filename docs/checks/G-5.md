# G-5 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| Variables 未定義でも Text Style 定義済みなら `tokens.json` が出力されること | OK | `src/export/tokensBuilder.ts`'s `buildTokens()` (line ~118): `if (supported.length === 0 && textStyles.length === 0) return null;` — this condition already existed from G-2 and correctly outputs when `textStyles.length > 0` regardless of `supported` (COLOR/FLOAT Variables). Covered by the pre-existing test "returns non-null when only text styles exist (no Variables)" in `src/export/__tests__/tokensBuilder.test.ts`. | PASS | Independent review confirmed the guard condition matches this requirement exactly. |
| どちらも未定義なら `tokens.json` が出力されないこと | OK | Same guard: `supported.length === 0 && textStyles.length === 0` → `null`. Covered by pre-existing tests "returns null when no variables" and "still returns null when both variables and text styles are empty". Also newly verified the STRING/BOOLEAN-only-variables-and-no-styles case is `null` (pre-existing test, line ~120) and, as the previously untested combination, that STRING/BOOLEAN-only variables **with** a Text Style present correctly returns non-null (new test added this task). | PASS | Confirmed the guard is exercised correctly across all 4 combinations of (supported Variables present/absent) × (Text Styles present/absent). |
| テストが全グリーンであること | OK | `bun run test` → `Test Files 10 passed (10)`, `Tests 151 passed (151)` (+1 from the new test added this task). `bun run build` succeeds with 0 errors. | PASS | Re-run independently, same result. |

## Scope note

The `tokensBuilder.ts` output-condition code itself required **no behavior change** — it was already correct from G-2 (which added the `textStyles.length === 0` half of the guard when introducing typography tokens, per the deferred scope note in G-2's steering entry). This task's actual work was: (1) closing a design-doc gap — `docs/telldes-design.md` had 3 stale occurrences of "Variablesが定義されている場合のみ出力" (the zip tree diagram, the 4.5.1 prose, and the 4.7.4 file list) that predated G-2 and didn't account for Text Styles also being able to trigger `tokens.json` output; all 3 updated to state the actual condition (supported Variables (COLOR/FLOAT) OR Text Style). (2) closing a test gap — added a test for the "unsupported (STRING/BOOLEAN) Variables present + Text Style present" combination, which wasn't previously covered by any existing case.

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Doc accuracy | PASS | New wording matches `buildTokens`'s actual guard condition exactly, including the STRING/BOOLEAN-exclusion detail and the "neither → no `tokens.json`" case. |
| Test meaningfulness | PASS | The new test exercises a genuinely distinct code path from the two pre-existing null-returning tests (neither had "unsupported variable present + text style present" together) — not a duplicate. |

## Expert Reviews (code changes only)

### Language Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Wording clarity | PASS | Japanese phrasing is grammatical and consistent with surrounding document terminology (「対応Variables」「トークン化対象外」「Text Style」 usage matches existing doc conventions). |

### Software-engineering Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Scope / completeness | PASS | Full-document grep for the stale phrase confirmed all 3 occurrences were updated and none were missed; other `tokens.json` mentions elsewhere in the doc don't repeat the stale claim and didn't need changes. |

## Overall Verdict

- Self-check: OK
- QA: PASS
- Language expert: PASS
- Software-engineering expert: PASS
- Ready for user review: Yes — per the project's standing autonomous-approval instruction, the user reviews all tasks together on the PR rather than per-task.
