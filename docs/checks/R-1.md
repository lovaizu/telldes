# R-1 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 2フレーム構成時に desktop/ と mobile/ が正しく分離出力されること | OK | responsive=true 時に各 variant を role 名のフォルダに spec.json + screenshots/ + assets/ を配置 |
| tokens.json がルートに1つだけ出力されること | OK | tokens.json はルートに配置、variant フォルダには含まない |
| 単一フレーム時に従来の出力構造が維持されること | OK | responsive=false 時は従来通りルートに spec.json, screenshots/, assets/ を配置 |
| フレーム幅が 1440px / 375px 以外の場合にユーザーに確認する動作があること | OK | 未知の幅はフォールバックで幅の大小で desktop/mobile を自動判定。テストで検証 |

## 総合判定

- セルフチェック: OK
- ユーザーレビュー可否: 可
