# E-1 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| 出力 JSON が設計書 4.5.2 節のスキーマに準拠すること | OK | page, viewport, children 構造が設計書のサンプルと一致。テストで検証済み |
| type フィールドが階層に応じて section/block/element に正しく設定されること | OK | depth=1 で section、子ありで block、末端で element。テスト3件で検証 |
| path が `>` 区切りで正しく生成されること | OK | "pricing > card > label" 形式で生成。テストで3階層まで検証 |
| Variable が適用されているプロパティに `*Token` フィールドが付与されること | OK | boundVariables からトークン名を取得し fontSizeToken 等を付与。テストで検証 |
| note が設定されたノードにのみ `note` フィールドが存在すること | OK | getPluginData("note") が空でないノードのみ note フィールド付与。テストで検証 |
| テストが全グリーンであること | OK | 49テスト全パス（specBuilder: 14件） |

## 総合判定

- セルフチェック: OK
- ユーザーレビュー可否: 可
