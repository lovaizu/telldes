# S-1 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| `bun run build` でエラーなくビルドが完了すること | OK | vite build × 2（UI + code）が正常終了。コンソール出力で確認 |
| `dist/code.js` と `dist/ui.html` が生成されること | OK | `ls dist/` で code.js (76B) と ui.html (29705B) の存在を確認 |
| Figma にプラグインとして読み込め、UIパネルが表示されること | - | ユーザーによる Figma 上での確認が必要 |
| UIに3つのタブ（チェック / note / 書き出し）が表示されること | OK | ui.html 内に「チェック」「note」「書き出し」の3ボタンが含まれることを grep で確認。Figma 上の実動作確認はユーザーに委任 |

## 総合判定

- セルフチェック: OK（Figma 実機確認を除く）
- ユーザーレビュー可否: 可（Figma でのプラグイン読み込み・UI 表示の確認をお願いします）
