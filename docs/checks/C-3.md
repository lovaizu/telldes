# C-3 完了条件チェック

## 完了条件チェックリスト

| 完了条件 | 判定 | 根拠 |
|---|---|---|
| チェック実行後に結果一覧が表示されること | OK | Run Review ボタンで code.ts がページ全ノードを走査し、結果を UI に postMessage。結果はリスト表示される |
| エラーと提案が視覚的に区別されていること | OK | エラー: 赤ボーダー（#f24822）＋ "Errors" ヘッダー、提案: 紫ボーダー（#7b61ff）＋ "Suggestions" ヘッダー |
| 結果クリックで対象ノードが選択され画面が移動すること | OK | クリックで select-node メッセージ送信 → code.ts が figma.currentPage.selection と figma.viewport.scrollAndZoomIntoView を実行 |
| エラーゼロ時に「パス」表示がされること | OK | results が空の場合 "All checks passed" を緑色で表示 |

## 総合判定

- セルフチェック: OK
- ユーザーレビュー可否: 可
