# U-4 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| `README.md` の Contents 行 `for frame "X"` の `X` が Figma 上の生のフレーム名と一致すること（フォルダ名にサニタイズや `-N` が入ったケースでも生名が出ること） | OK | `ExportFrame` に `name`（Figma の生名）と `folderName`（zip フォルダ名）を分離（`src/messages.ts`）。`ReadmeFrame` も同じ2本立てにし、`frameContentsLine` は `` - `${folderName}/` — … for frame "${name}" `` を出す（`src/export/readmeBuilder.ts` `frameContentsLine`）。テスト: `readmeBuilder.test.ts`「names the folder as the zip spells it and the frame as Figma does」（folderName `Desktop - Home-2` / 生名 `Desktop / Home`）、`zipBuilder.test.ts`「names the frame in README as Figma spells it, not as the folder does」（実際に組み立てた zip の README 本文で検証）。変異 M1（`for frame "${folderName}"` に戻す）で2件が落ちることを確認。**レビュー指摘対応で追加**: 空名フレームの end-to-end（`zipBuilder.test.ts`「writes a nameless frame under the fallback folder, naming it as Figma does」で `` - `frame/` — spec.json for frame "" `` を実 zip の README 本文で固定、`code.test.ts`「names a frame Figma left blank by the fallback, keeping the raw name」で `{ name: "", folderName: "frame" }` を固定）。設計書 4.7.4 に「Contents の各フレーム行はフォルダ名（4.5.2 正規化後）と Figma 上の生名の両方を示す」旨を明記（F1）。**修正ラウンド2**: `readmeBuilder.ts` / `zipBuilder.ts` とその両テストは無改変（`git status` で確認）。フォルダ名の出所が `resolveExportScope` に替わった後も `readmeBuilder.test.ts` 18件・`zipBuilder.test.ts` 15件がアサーション無改変でパス **修正ラウンド3**: `src/messages.ts` はコメント行のみの変更（`git diff -U0 -- src/messages.ts` の ± 行がすべてコメント行で、型宣言・フィールド・union は無改変）。`readmeBuilder.ts` / `zipBuilder.ts` とその両テストは今回も無改変で、`readmeBuilder.test.ts` 18件・`zipBuilder.test.ts` 15件がパス。さらに変更前後で `bun run build` の成果物（`dist/code.js` / `dist/ui.html`）が SHA-1 一致し、Contents 行の出力経路がまったく動いていないことを実物で確認 | OK | 4軸とも OK。`readmeBuilder.ts:42` が `folderName` と `name` を別スロットで出力。変異「`for frame "${folderName}"` に戻す」で3件、「`code.ts` で生名を折り畳む」で2件が落ちることを4軸が独立に実測。サニタイズ例・`-N` 例・空名例の3ケースがテスト済み |
| `src/export/zipBuilder.ts` が `resolveFrameFolderNames` を呼ばず、zip のフォルダ名が `ExportFrame` に載った値そのものであること | OK | `zipBuilder.ts` から `layerPath` の import ごと削除し、`root.folder(frame.folderName)` を使う（`src/export/zipBuilder.ts`）。`resolveFrameFolderNames` 自体を `layerPath.ts` から削除（第2実装の消滅）。`grep -rn resolveFrameFolderNames src` は 0 件。フォルダ名の生成は `resolvePageRootNames`（本対応で `src/export/exportScope.ts` へ移設）1箇所で、`code.ts` がフレームに載せ、README のレイヤーパス先頭セグメントも同じ Map から出る ― 設計書 4.7.2「同一の関数で生成」が構造として成立。テスト: `zipBuilder.test.ts`「writes each frame into the folder name the message carries」、`code.test.ts`「stamps each frame with the folder name the README layer paths are rooted at」（`folderName` と除外物レイヤーパスの先頭セグメントの一致を実値で突き合わせ）。変異 M2（`root.folder(frame.name)`）・M3（`folderName: frame.name`）で各1件が落ちる。**レビュー指摘対応**: (A-2) `collectExclusions` が `rootNames` を引数で受け取る形にし、命名パスの実行を1回に統一（`ExclusionScanInput.rootNames`）。`exclusions.ts` は `resolvePageRootSegmentNames` を import しなくなり、2度目の走査が構造的に不可能。`code.test.ts`「roots the README layer paths at the very names it stamps on the folders」で folderName と除外物パス先頭の一致を実値で突き合わせ（変異 M17 で検知）。(A-3) `src/export/exportScope.ts` を新設し `isExportedFrame` / `resolvePageRootNames` を移設 ― 4.5.2 の命名規約を 4.7.2 のモジュールが所有する逆向き依存を解消。(B-2) `zipBuilder.ts` に重複 `folderName` の throw を追加（`data.exclusions` 欠落と同じ方針）。**修正ラウンド2**: `grep -rn resolveFrameFolderNames src` は 0 件のまま。加えて命名と「書き出し対象フレームの導出」を1つの値 `ExportScope`（`src/export/exportScope.ts`）にまとめ、`code.ts` は `resolveExportScope(page.children)` を1回呼ぶだけ、`collectExclusions` は `{ scope, variables, textStyles }` を受け取る形にした。ペアがズレる引数の組み合わせ自体が存在しなくなった。変異 M-A（`resolveExportScope(page.children.filter(isExportedFrame))`）で `code.test.ts` 2件が落ちる（ラウンド1では同種の変異が243件全パスだった）。`RESERVED_ROOT_NAMES` とページ直下命名一式も `layerPath.ts` から `exportScope.ts` へ移設（D2） **修正ラウンド3**: `zipBuilder.ts` は無改変（`git status`）。`root.folder(frame.folderName)` のまま `resolveFrameFolderNames` の呼び出しはなく、`zipBuilder.test.ts` 15件がパス。ビルド成果物も HEAD とバイト一致 | OK | 4軸とも OK。`grep -rn resolveFrameFolderNames src` は0件（関数ごと削除）。`zipBuilder.ts:65` は `root.folder(frame.folderName)`。変異「`frame.name` に差し替え」で2件失敗 |
| `src/messages.ts` の `CheckErrorMessage` が `src/code.ts` から参照されていること | OK | `src/code.ts` 冒頭で `CheckErrorMessage` / `CheckResultsMessage` を型 import し、`run-checks` の post 2箇所を `const message: CheckResultsMessage = …` / `const message: CheckErrorMessage = …` として注釈。`npx tsc --noEmit` は `src/code.ts` と `src/export/*` にエラー 0（既存の App.tsx の JSX 型エラーと `sizingChecks.ts` の TS2367 は本タスク前から存在。リポジトリに tsc ステップはない）。**修正ラウンド2**: `code.ts` は `CheckErrorMessage` / `CheckResultsMessage` に加え `SelectionNote` / `SelectionNoteMessage` / `NoteSavedMessage` / `ExportErrorMessage` も `messages.ts` から参照する。UI 側は新設の `PluginMessage` union を受け取る **修正ラウンド3**: コメント削減後も `src/code.ts:8` の型 import と `src/code.ts:88` の `const message: CheckErrorMessage` は無改変（`grep -n CheckErrorMessage src/code.ts` で2箇所を確認）。`messages.ts` の変更はコメント行のみで、`CheckErrorMessage` の宣言自体も無改変 | OK | 4軸とも OK。`code.ts:8` で型 import、`code.ts:88` で `const message: CheckErrorMessage` として使用。変異「catch から check-error 送信を削除」で `code.test.ts` 1件失敗（飾りの注釈ではなく送信経路が試験されている） |
| `check-error` を受信した直後の Review タブに、前回の実行結果が表示されていないこと | OK | `reviewOutcome()` が1回の実行結果を丸ごと返し、`check-error` では `{ results: [], hasRun: false, error: message }`。ハンドラは `check-results` / `check-error` を1本にまとめ、4つの signal すべてを毎回更新。パネルは `hasRun()` ゲートの内側だけを描くので、`hasRun=false` では結果リストも「All checks passed」も出ず、失敗バナーのみが残る。テスト: `src/__tests__/App.test.ts`。変異 M4（`hasRun: true`）・M4b（`check-error` が `check-results` 側の分岐を取り、前回結果を保持）でいずれも検知。描画そのものは DOM テスト環境（jsdom 等）が無く依存追加も禁止のため、描画に載る値を純関数として切り出して固定する方針を取った。**レビュー指摘対応**: (B-1) `window.onmessage` の配線そのものを `applyReviewOutcome(msg, setters)` として切り出し、分岐条件と4つの setter 呼び出しを DOM なしで固定（`App.test.ts` 3件）。レビューが「生き残る」と指摘した4変異 M10〜M13 がすべて落ちることを実測。(D) 内側 `<Show>` を撤去した結果生じた「結果あり・エラー0件で `Errors (0)` と空 `<ul>`」のデッドパスを、`groupResultsByLevel()` によるレベル別描画に置き換え ― 見出しの件数は必ずその直下に並ぶ件数と一致し、どのレベルの結果も落ちない（`App.test.ts` 3件、変異 M16 で検知）。**修正ラウンド2**: `window.onmessage` の本体を `handlePluginMessage(msg, handlers)` として切り出し（`src/App.tsx`）、`applyReviewOutcome` の呼び出しを含む全分岐を DOM なしで固定（`App.test.ts` に9件追加）。変異 M-C（`applyReviewOutcome` の呼び出し行を丸ごと削除）で2件が落ちる（ラウンド1では243件全パスだった）。`export-error` / `note-saved` / `selection-note` の分岐も同時に閉じた（M-D〜M-G） **修正ラウンド3**: `App.tsx` は無改変（`git status` でワークツリーにクリーン）。`App.test.ts` の変更は `handle()` fixture の `noteSaved` 初期値を引数で渡せるようにした分だけで、`reviewOutcome` / `applyReviewOutcome` / `handlePluginMessage` の `check-error` 系アサーション5件はそのままパス | OK | 4軸とも OK（ただし検証はロジック層まで）。`App.tsx:32-34` が `results: []` / `hasRun: false` を返し、描画は `<Show when={hasRun()}>` でゲート。変異「`hasRun: true`」で2件、「`setResults` 行削除」で3件、「`setRunning(false)` 削除」で6件失敗。**描画層（JSX）は無検証で、`when={true}` の変異が生存する** — jsdom 未導入による既知の残存リスクで、ユーザー判断待ち |
| 0フレーム書き出し・多段サブパスのトークン名衝突・DOCUMENT 終端のレイヤーパスを対象とするテストが存在し、パスすること | OK | 0フレーム: `zipBuilder.test.ts`「still writes the four root files when the page has no exportable frame」（ルート4ファイルのみ・Contents にフレーム行なし・`prompt.md` はデフォルト 1440・`steering.md` は `(no sections found)`）と `code.test.ts`「still posts an export when the page holds no exportable frame」（`export-error` なし、`frames: []` を post）。多段サブパス: `exclusions.test.ts`「matches a multi-level sub-path against the Text Style's full name」（`typography/a/b` × Text Style `a/b` が衝突）と、先頭セグメントだけ一致する場合は非衝突の対をなすテスト。DOCUMENT 終端: `exclusions.test.ts`「stops at a DOCUMENT parent, which is no more a layer than a PAGE is」。変異 M5（`parts[1]`）で2件、M6（`current.type !== "DOCUMENT"` 削除）で1件、M7（`?? 1440` → `?? 375`）で1件が落ちる。**レビュー指摘対応**: (E) `baseSegment` が `.` / `..` を素通ししていた既存バグを修正。ドットのみの名前を `frame` に落とし、zip ルートの予約名（`README.md` / `prompt.md` / `steering.md` / `tokens.json`）を `used` 集合に先込みして `-N` を取らせる。`layerPath.test.ts` に5件追加（変異 M14 / M15 で検知）。実 zip で確認: フレーム名 `..` / `README.md` / `"  "` の3枚で、エントリは `telldes-export/frame/`・`telldes-export/README.md-2/`・`telldes-export/frame-2/` となり、`telldes-export/../` も `README.md` の同名衝突も消滅。設計書 4.5.2 の正規化規約に規則を追記（F3）。**修正ラウンド2**: 当該3テストは無改変のままパス。大文字小文字の衝突（`Home` と `home`、`readme.md` と予約名 `README.md`）という新しい境界を `exportScope.test.ts` に3件追加 **修正ラウンド3**: 3テストとも無改変でパス — 0フレーム: `zipBuilder.test.ts`「still writes the four root files when the page has no exportable frame」、多段サブパスのトークン名衝突: `exclusions.test.ts`「matches a multi-level sub-path against the Text Style's full name」＋「ignores a multi-level sub-path whose Text Style matches only its first segment」、DOCUMENT 終端: `exclusions.test.ts`「stops at a DOCUMENT parent, which is no more a layer than a PAGE is」 | OK | 4軸とも OK。0フレーム: `zipBuilder.test.ts:135` + `code.test.ts:311`。多段サブパス: `exclusions.test.ts:359`（`typography/a/b` × Text Style `a/b`）＋否定ケース `:368`。DOCUMENT 終端: `exclusions.test.ts:444`。変異3種すべて kill |
| 新規モジュールのコメント行比率が既存モジュールの範囲（5〜13%）に収まっていること | OK | 再計測（コメント行 ÷ 非空行、前回と同一の算出式）: `exclusions.ts` **12.5%**（32/255）、`zipBuilder.ts` **12.4%**（11/89）、`readmeBuilder.ts` **10.9%**（12/110）は範囲内。`layerPath.ts` **17.6%**（12/68）と新設 `exportScope.ts` **18.2%**（2/11）は範囲外。理由と判断: `layerPath.ts` のコメント実数は 7→12 行で、増分5行はすべて本レビューが復元/追加を指示したもの（S-1 の「index 0 から再生する理由」2行、E の「ドットのみはパス操作」1行、E の「予約名を先込みする理由」2行）。`exportScope.ts` は非空11行に対し export 2本の1行 JSDoc が2行で、13% は「exportのどちらか一方を無文書にする」ことと同義になる（S-4 は export シンボルに `/** */` を要求）。どちらも比率を満たすには本レビューが明示的に求めた内容を削る必要があるため、実数を優先した。分母の大きい3モジュールは範囲内に戻してある。**修正ラウンド2**: steering.md の当該基準は 2026-09-16 に「同程度の規模の既存モジュールと同水準」へ改められているため、その基準で再計測（コメント行 ÷ 非空行）。`exportScope.ts` **24.1%**（14/58）で、同規模の既存モジュール `src/util/color.ts` 23.3%（7/30）・`src/export/layerPath.ts` 18.4%（7/38）・`src/checks/traversal.ts` 17.2%（5/29）と同水準。他は `exclusions.ts` 12.9%（33/255、着手時 12.5%）、`code.ts` 21.0%（着手時 23.3%）、`App.tsx` 7.0%（着手時 5.4%、追加分は新設2関数の JSDoc） **修正ラウンド3**: `messages.ts` を **49.5%（50/101）→ 21.5%（14/65）** に下げた。冒頭の15行ブロックを3行の `//` に畳み、`ExportDataMessage.exclusions` / `CheckErrorMessage` / `ExportErrorMessage` で3回繰り返していた同じ論旨を冒頭1箇所に集約、型宣言から読み取れるだけの説明（`ExportDataMessage` / `CheckResultsMessage` / `NoteSavedMessage` の見出し行など）を削除。残したのは型から読み取れない why だけ（`name` と `folderName` の区別、`exclusions` が optional でない理由）。`exportScope.ts` は V-3 の `readonly` に1行の why を足して 24.1%→25.4%（15/59）。全モジュール再計測（コメント行 ÷ 非空行、ブロックコメントの各行も1行）: `messages.ts` 21.5%（14/65）、`exportScope.ts` 25.4%（15/59）、`readmeBuilder.ts` 10.9%（12/110）、`tokensBuilder.ts` 14.8%（19/128）、`assetExporter.ts` 5.3%（5/95）、`structureChecks.ts` 5.8%（7/121）、`traversal.ts` 17.2%（5/29）、`util/color.ts` 23.3%（7/30）、`util/typography.ts` 31.7%（20/63）、`layerPath.ts` 18.4%（7/38）、`exclusions.ts` 12.9%（33/255）、`zipBuilder.ts` 12.4%（11/89）、`specBuilder.ts` 8.2%（39/475）、`code.ts` 21.0%（39/186）、`App.tsx` 7.2%（34/474）、`checks/types.ts` 52.9%（9/17）。宣言だけのモジュールという同型の既存比較対象は `checks/types.ts` 52.9% と `util/color.ts` 23.3% / `util/typography.ts` 31.7% で、`messages.ts` 21.5% はそのどれよりも低い。完了条件が名指しした 40〜55% の帯（既存の3〜8倍）からは外れており、`code.ts` 21.0%・`layerPath.ts` 18.4% と同水準。なお `readmeBuilder.ts` 級の 10〜15% には届いていない — そこまで落とすにはコメントを9行以下にする必要があり（非コメント行が51行のため）、`name`/`folderName` の区別と `exclusions` 必須の理由という型から読めない why を捨てることになるので、比率だけを基準にしないという条件の但し書きに従って残した | NG | **4軸一致で NG**。`src/messages.ts` が 44.6〜49.5%（計測式の差。コメント50行）で、条件が名指しする 40〜55% の帯に残る。同規模の既存モジュール（`readmeBuilder.ts` 110行 10.9%、`tokensBuilder.ts` 128行 14.8%、`assetExporter.ts` 95行 5.3%）の4〜9倍。条件の但し書き「行数の少ないモジュールでは比率だけを基準にしない」は非空101行の同ファイルには当たらない。U-1 時点からコメント**実数は 32→50 行に増加**しており、比率低下は本文が増えた分。指摘された重複: 冒頭ブロック（:4-18）の論旨が `ExportDataMessage.exclusions`（:47-52）/ `CheckErrorMessage`（:62-69）/ `ExportErrorMessage`（:94-99）で3回繰り返されている。他の5モジュールは解消済み（exportScope 21〜24% / exclusions 12.9% / layerPath 18.4% / readmeBuilder 10.9% / zipBuilder 12.4%、既存 `traversal.ts` 17.2% ・ `util/color.ts` 23.3% と同水準） |
| 新たな問題が持ち込まれていないこと — (a) 既存テストが全グリーン、(b) 除外物の検出内容と README の除外物セクションの出力内容が不変 | OK | (a) `bun run test` 225/225 グリーン（着手時 217、追加 8）。`bun run build`（UI + plugin code 両方）も成功。(b) `exclusions.ts` のコメント以外の差分は `resolveRootSegmentNames` → `export resolvePageRootNames` の改名2行のみ（検出ロジックは1行も変えていない）、`readmeBuilder.ts` のコメント以外の差分は `ReadmeFrame.folderName` 追加と Contents 行1行のみで、`exclusionLines` / `usageLine` / `exclusionBullet` / `buildReadme` 本体は不変。除外物セクションの文言を実値で固定している既存テスト（`readmeBuilder.test.ts` 17件、`exclusions.test.ts` の end-to-end、`zipBuilder.test.ts`、`code.test.ts` の `export-data.exclusions`）は無改変で全てパス。**再検証**: `bun run test` 243/243 グリーン（着手時 225、追加 18）、`bun run build`（UI + plugin code）成功。除外物の検出ロジックは A-2 の引数化（`rootNames` を受け取るだけ）以外に変更なし ― `findColorStyleUsage` / `findStringBooleanVariableUsage` / `findBareRootComponents` / `findTypographyTokenCollisions` / `layerPathOf` / `segmentName` の本体は1行も変えていない。`readmeBuilder.ts` の変更はファイル頭コメントの位置（S-5）のみ。除外物セクションの文言を実値で固定する既存テスト（`readmeBuilder.test.ts` 18件、`exclusions.test.ts` の end-to-end、`zipBuilder.test.ts`、`code.test.ts` の `export-data.exclusions`）はアサーション無改変で全てパス。**修正ラウンド2**: (a) `bun run test` 257/257 グリーン（着手時 243）、`bun run build` 両ビルド成功。(b) `readmeBuilder.ts` / `zipBuilder.ts` は無改変、`exclusions.ts` の差分は入力の受け取り方（`scope` 1本化）とコメントのみで検出ロジックは1行も変えていない。`exclusions.test.ts` の差分もヘルパ2箇所のみでアサーションは無改変（59件パス）。加えて `npx tsc --noEmit` のエラー総数が 84→82 に減り、`App.test.ts` 分の TS2353 2件（`applyReviewOutcome` のパラメータ型が狭すぎた分）は 0 になった **修正ラウンド3**: (a) `bun run test` 257/257 グリーン、`bun run build`（UI + plugin code）も成功。`tsc --noEmit` のエラーは変更前後で完全一致（82行、diff 空。いずれも既存の JSX / Figma 型由来で、`messages.ts` / `exportScope.ts` / `exclusions.ts` / `code.ts` には1件もない）。(b) 変更前（HEAD）と変更後で `bun run build` の成果物が SHA-1 バイト一致（`dist/code.js` = `610f3927…`、`dist/ui.html` = `dea9bfab…`）。今回の変更はコメント削減と型の `readonly` 付与とテスト fixture だけで、実行されるコードは1バイトも動いていないため、除外物の検出内容も README の除外物セクションの出力内容も定義上不変 | OK | 4軸とも OK。(a) `bun run test` 257/257 green、`bun run build` 両方成功、`tsc --noEmit` 82件は全て既存由来で本差分の新規・変更モジュールには0件。(b) `exclusions.ts` の検出4関数・`UsageGrouper`・`dedupe` は `ad80720` 版と論理が一致（差分は入力の配管とコメントのみ）、`readmeBuilder.ts` の `exclusionLines`/`usageLine`/`exclusionBullet` も無変更 |

### レビュー指摘への対応（A〜F）

| 項目 | 対応 |
|---|---|
| A-1 | `src/code.ts`: `rootNames.get(frame.id)!` を明示 throw に。既存 catch が `export-error` に変換。テスト: `code.test.ts`「fails the export rather than piling every frame into the export root」（変異 M8） |
| A-2 | `collectExclusions` が `ExclusionScanInput.rootNames` として解決済み Map を受け取る。`exclusions.ts` から `resolvePageRootSegmentNames` の import が消え、命名パスは `code.ts` の1回だけ。`code.ts` のコメントも事実に合わせて書き直し（変異 M17） |
| A-3 | `src/export/exportScope.ts` を新設し `isExportedFrame` / `resolvePageRootNames` を移設。`exclusions.ts`（4.7.2）と `code.ts` の双方がそこから import。テストも `exportScope.test.ts` へ分離 |
| B-1 | `applyReviewOutcome(msg, setters)` を App.tsx から export し、`window.onmessage` は1行の呼び出しに。`App.test.ts` 3件で分岐条件と4 setter を固定（変異 M10〜M13 がすべて落ちる） |
| B-2 | `zipBuilder.ts` で重複 `folderName` を throw。テスト1件（変異 M9） |
| B-3 | 空名フレームの README 行を `zipBuilder.test.ts` / `code.test.ts` で固定。`exclusions.test.ts` の describe を「layer-path roots use the page-root segment names」へ改名（突き合わせ assertion は `code.test.ts` が実値で持つ） |
| C | S-1（`uniqueChildName` の index 0 からの再生）、S-2（`emptyExclusionReport` はテスト専用）、S-3（`styleName: null` は `figma.mixed`）を復元。S-4: `layerPath.ts` / `exclusions.ts` の export・インターフェイスメンバを `/** */` に統一。S-5: ファイル頭コメントを import の上へ揃えた（`exclusions.ts` / `readmeBuilder.ts`）。削って正しかったもの（過去バグ事例・`dedupe` の経緯・第2実装のドリフト）は戻していない |
| D | `Errors (0)` と空 `<ul>` のデッドパスを撤去。`groupResultsByLevel()` でレベル別に見出し＋リストを描き、件数は必ず直下の件数と一致。`error` 以外のレベルも見出しを得て落ちない（変異 M16） |
| E | `baseSegment` でドットのみの名前を `frame` に。`used` 集合に zip ルートの予約名4件を先込みし、一致したフレームは `-N` を取る。`layerPath.test.ts` に5件（変異 M14 / M15）。実 zip で `telldes-export/../` と `README.md` の file/folder 同居が消えたことを確認 |
| F | 設計書: 4.7.4 に Contents 行がフォルダ名と生名の両方を示す旨を追記（F1）、4.7.2 の「同一の関数で生成」を「同一の走査で一度だけ決定し双方へ同じ値を渡す」に修正（F2）、4.5.2 の正規化規約にドットのみ・予約名の規則を追加（F3）、4.7.2「実行失敗の扱い」に「前回の実行結果の表示を消す」を追記して `App.tsx` の引用を裏づけた（F4） |

### 検証手法の適用状況（Method）

- テストファースト: 適用した。実装前に失敗するテストを先に書き、`bun run test` で 5 件の失敗（`readmeBuilder` 1 / `zipBuilder` 1 / `code` 1 / `App` 2）を確認してから実装に着手し、225/225 グリーンで完了とした。既存挙動を固定するだけのテスト（0フレーム・多段サブパス衝突・DOCUMENT 終端）は最初から通るため、下記の変異テストで「実際に噛んでいること」を確認した。
- レビュー指摘対応でも同じ順序を守った。E → 3 件失敗を確認して実装、B-2 → 1 件失敗、B-1 / D → 6 件失敗、A-1 / A-2 → 各1件失敗を確認してから実装。最終 243/243 グリーン、`bun run build` 成功。
- 実物での確認: フレーム名 `..` / `README.md` / `"  "` の3枚を `resolvePageRootNames` → `buildExportZip` に通して実 zip のエントリ一覧と README 本文をダンプし、`telldes-export/` の外に出るエントリも `README.md` の file/folder 同居も無いことを確認した（確認用のテストファイルは削除済み）。
- 変異テスト: カバレッジプロバイダ未導入・依存追加禁止のため `--coverage` の代わりに実施。前回 8 件＋今回 11 件、すべて検知（各実行後にファイルを復元）。
  - M1 `readmeBuilder.ts`: Contents 行を `for frame "${folderName}"` に戻す → 2 件失敗
  - M2 `zipBuilder.ts`: `root.folder(frame.name)` にする → 1 件失敗
  - M3 `code.ts`: `folderName: frame.name`（生名をそのまま載せる） → 1 件失敗
  - M4 `App.tsx`: `check-error` で `hasRun: true` → 1 件失敗
  - M4b `App.tsx`: `check-error` が `check-results` 側の分岐を取る（前回結果が残る） → 1 件失敗
  - M5 `exclusions.ts`: 衝突判定を `parts.slice(1).join("/")` → `parts[1]` → 2 件失敗
  - M6 `exclusions.ts`: `layerPathOf` の `current.type !== "DOCUMENT"` を削除 → 1 件失敗
  - M7 `zipBuilder.ts`: 0フレーム時のビューポート既定値 `1440` → `375` → 1 件失敗
  - M8 `code.ts`: 欠落フォルダ名の throw を削って `!` に戻す → 1 件失敗
  - M9 `zipBuilder.ts`: 重複 `folderName` の throw を削除 → 1 件失敗
  - M10 `App.tsx`: `setters.setResults(...)` を削除（前回結果が残る） → 2 件失敗
  - M11 `App.tsx`: `setHasRun(outcome.hasRun)` → `setHasRun(true)` → 1 件失敗
  - M12 `App.tsx`: `setCheckError(outcome.error)` → `setCheckError("")` → 1 件失敗
  - M13 `App.tsx`: 分岐条件から `msg.type === "check-error"` を落とす → 1 件失敗
  - M14 `layerPath.ts`: ドットのみの名前を素通しさせる → 2 件失敗
  - M15 `layerPath.ts`: `used` への予約名の先込みをやめる → 1 件失敗
  - M16 `App.tsx`: `groupResultsByLevel` が error だけを拾う → 1 件失敗
  - M17 `code.ts`: 除外物走査に2度目の等価な命名パスを渡す（`[...page.children].reverse()`） → 1 件失敗
  - M18 `layerPath.ts`: `uniqueChildName` の再生をカウンタ1本に「最適化」 → 5 件失敗（S-1 のコメントが守っている挙動）

### 修正ラウンド2の適用状況（Method）

- テストファースト: 4件すべてで先に落ちるテストを書いた。件1・件2 → `exportScope.test.ts` を新 API（`resolveExportScope` / `folderNameOf`）で書き直して18件失敗を確認してから実装。件3・件4 → `App.test.ts` に `handlePluginMessage` の9件を追加して9件失敗を確認してから実装。
- レビュアーが当てた変異を実際にコードへ当てて確認した（各実行後にファイルを復元）。今回の狙いは「自分の想定した変異だけでなく、指摘された変異そのもので落ちること」の実測。

  | 変異 | 内容 | 結果 |
  |---|---|---|
  | M-A | `src/code.ts`: `resolveExportScope(page.children)` → `resolveExportScope(page.children.filter(isExportedFrame))`（レビュアー指摘の変異そのもの） | `code.test.ts` 2件失敗 |
  | M-B | `src/export/exportScope.ts`: `used` の照合を大文字小文字区別に戻す | `exportScope.test.ts` 3件失敗 |
  | M-B2 | `claimUnique` が小文字化した綴りを返す（原名を保たない） | 30件失敗 |
  | M-C | `src/App.tsx`: `applyReviewOutcome(msg, handlers);` の呼び出し行を削除（レビュアー指摘の変異そのもの） | `App.test.ts` 2件失敗 |
  | M-D | `note-saved` の `msg.nodeId === handlers.selectedNodeId()` 比較を外す（遅延 ack を受理） | 1件失敗 |
  | M-E | `export-error` の分岐を削除 | 1件失敗 |
  | M-F | `export-data` の分岐を削除 | 1件失敗 |
  | M-G | `selection-note` の `setNoteText` を削除 | 2件失敗 |
  | M-H | `folderNameOf` の throw を `?? frame.name` のフォールバックに | 2件失敗 |

- テスト件数: 243 → 257。内訳は `App.test.ts` +9（`handlePluginMessage`）、`exportScope.test.ts` 4 → 20（うち11件は `layerPath.test.ts` から移設、7件が新規、1件は予約名の大小文字の期待値を新挙動へ反転して書き換え）、`layerPath.test.ts` 15 → 4。`code.test.ts` は件数は据え置きでモック対象を `resolveExportScope` へ変更。
- カバレッジ: `bunx vitest run --coverage` は `@vitest/coverage-v8` 未導入で実行できず（`MISSING DEPENDENCY`）。依存追加は環境変更にあたるため行っていない。**カバレッジツール未導入のため未測定**。代わりの根拠は上の変異テスト9件（すべて検知）。
- 残存リスク（既知・今回は閉じない）: 描画層は DOM テスト環境（jsdom）が無いため変異が生存する。具体的には `src/App.tsx` の `<Show when={hasRun()}>` を `when={true}` にしても257件全パスする。閉じるには jsdom の導入（環境変更）が必要でユーザー確認を要するため、今回はコードでの対応をしていない。`hasRun` に載る値そのものは `reviewOutcome` / `applyReviewOutcome` / `handlePluginMessage` のテストで固定してある。

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Verification approach meaningful to the objective (checks the right thing, not just "passed") | OK | 変異テストで裏づけられている。QA 軸は10件中9件、Verification 軸は63件中54件、Craft 軸は28件全件が kill。`code.test.ts` は `figma` をスタブして `code.ts` を実際に import・駆動し、スコープ解決→除外走査→post されるメッセージ列まで通している（エラーゲートの `return` 削除・レベルフィルタ削除も検知）。「動いた」確認ではなく境界を押さえている。生存変異のうち実害のあるものは描画層（DOM テスト不在）と下記2件 |

## Expert Reviews (axes the task needs)

### Design Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Approach/structure fits | OK | 依存は一方向で循環なし。`exportScope.ts` は何も import しない葉モジュールで「ページ直下の命名」という1責務に集約。`exclusions.ts → exportScope.ts` は type-only import でランタイム依存も生じない。`messages.ts → exclusions.ts` も type-only で、UI バンドルに Figma API 依存が流れ込まない（`dist/code.js` 18.5kB でビルド確認） |
| System-wide integrity (interfaces, cross-doc consistency) | NG | 設計書 4.5.2 の規約1〜5は `exportScope.ts:12-38` に1対1で対応し、4.7.2・4.7.4 とも一致。ただし (a) `ExportScope` は不変条件を型では保証しておらず、`frames` のみ `readonly` が付いていない（他2フィールドは付いている）。(b) `isExportedFrame` と同じ判定が `specBuilder.ts:511` に独立して存在する。(c) 0フレーム書き出しの挙動が設計書に記載されていない |

### Craft Expert (coding)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Medium-specific best practice | NG | 重大な誤りはなく、境界（空名・ドットのみ・予約ファイル名・大小違い・0フレーム・DOCUMENT 終端・分離ツリー）はよく塞がれている。残る穴は `baseSegment` が末尾ドットと Windows 禁止文字（`: * ? " < > |`）を無害化しないこと（`Home.` と `Home` が Windows で同一パスに解決し silent overwrite）、`zipBuilder` の重複ガードが大小文字を区別すること、`PluginMessage` union に網羅性チェックがないこと |
| Consistency with existing style | NG | 命名・型の使い方・モジュール分割の粒度（`exportScope.ts` 66行は `util/color.ts` 32行、`checks/traversal.ts` 31行と同水準）は既存と揃っている。外れているのは `messages.ts` のコメント密度（完了条件6）と、`reviewOutcome`→`applyReviewOutcome`→`handlePluginMessage` の三段が Review だけ「無条件呼び出し＋関数内早期 return」で他4分岐（`if` で直接分岐）と非対称なこと |

### Verification Expert (test)

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Artifact actually checked (tests run / claims verified / flow traced) | OK | `bun run test` 257/257 green、`it.skip` / `describe.only` / 空アサーションなし。投入変異63件中54件が kill。GWT の形（Given=fixture、When=1操作、Then=expect）が保たれ、`it` 名が挙動を述べる文になっている |
| Coverage (edge cases / claims / steps) | NG | 境界・空・最大・エラー経路・型変換は広くカバー済み（`-N` 採番の開始値、`layerCount` 単複、例示パス上限3、`figma.mixed`、削除済み Variable、分離ツリー、各種 throw）。穴は3点: (1) `App.test.ts:126` の fixture が `noteSaved: false` 始まりのため `:166` のアサーションが初期値をなぞるだけで空振りし、`App.tsx:87` の `setNoteSaved(false)` を削除しても検知されない。(2) `exportScope.ts:13` のバックスラッシュ無害化にテストがなく、`\\` 置換を削っても全グリーン。(3) Solid コンポーネント本体（配線と JSX）が完全に無検証で、`<Show when={hasRun()}>` → `when={true}` を含む6変異が生存 |

## Overall Verdict

- Self-check: OK（修正ラウンド2で7項目すべて OK。コメント行比率は改定後の基準で再計測して OK）。**修正ラウンド3**: V-1（`messages.ts` のコメント比率 49.5%→21.5%）・V-2（`App.test.ts` の `noteSaved` fixture が `App.tsx` の `setNoteSaved(false)` 削除で落ちるようになった）・V-3（`ExportScope.frames` を `readonly` 化、`push` が TS2339 で弾かれることを確認）を解消。7項目すべて OK
- QA: NG（完了条件6のみ。①〜⑤⑦は変異耐性つきで達成）
- Design expert: NG（完了条件6＋システム整合性3点）
- Craft expert: NG（完了条件6＋ベストプラクティス/一貫性の軽微な指摘）
- Verification expert: OK（総合 pass。完了条件6は「要判断」、カバレッジに穴3点）
- Ready to check off: No（完了条件6が4軸一致で未達。修正ラウンド3で解消する）

## 修正ラウンド2 再レビューの triage（2026-09-19）

4軸を独立サブエージェントで再レビュー。各軸には `ad80720..HEAD` の差分・完了条件の逐語コピー・観点チェックリストのみを渡し、本ファイルは読ませていない。

### Valid（修正ラウンド3で潰す）

| # | 指摘 | 一致軸 | 根拠 |
|---|---|---|---|
| V-1 | `src/messages.ts` のコメント比率が 40〜55% の帯に残る | QA / Craft / Verification / Design（4軸） | 完了条件6そのもの。冒頭ブロックの論旨が3つの interface コメントで重複 |
| V-2 | `App.test.ts` の `noteSaved` fixture が初期値 `false` で空振りし、`App.tsx:87` を守れていない | Verification | 修正ラウンド2で本タスクが追加したテストの欠陥。テスト側だけで閉じる |
| V-3 | `ExportScope.frames` のみ `readonly` が付いていない | Craft / Design（2軸） | 本タスクが新設した型。他2フィールドと不揃いで、push すると `folderNameOf` の前提が壊れる |

### Invalid（完了条件の外。U-5 に積む／既に積んである）

| # | 指摘 | 判定根拠 |
|---|---|---|
| I-1 | `zipBuilder` の重複ガードが大小文字を区別する | U-4 の完了条件に含まれない。U-5「`zipBuilder` の境界ガードを揃える」に統合。`exportScope` 側が大小無視で一意化するため `code.ts` 経由では到達不能 |
| I-2 | `RESERVED_ROOT_NAMES` と zip 実体の一致が無検証 | 同上。U-5 に既出（Craft C-4） |
| I-3 | `baseSegment` のバックスラッシュ無害化にテストがない | U-5 に既出（Verification NG-3）。ラウンド2で `layerPath.ts` → `exportScope.ts` へ移設されたため U-5 の参照先を更新 |
| I-4 | `baseSegment` が末尾ドット・Windows 禁止文字を無害化しない | 完了条件の外。設計書 4.5.2 の改訂を伴うため U-5 に新項目として追加 |
| I-5 | `isExportedFrame` と同じ判定が `specBuilder.ts:511` に二重にある | 完了条件の外。U-4 以前から存在。U-5 に新項目として追加 |
| I-6 | `data.exclusions` 欠落時の throw が `readmeBuilder` の TypeError に依存 | 完了条件の外。U-5「境界ガードを揃える」に統合 |
| I-7 | 0フレーム書き出しの挙動が設計書にない | 完了条件の外。U-5 に新項目として追加 |
| I-8 | `PluginMessage` union に網羅性チェックがない | 完了条件の外。U-5「`tsc --noEmit` をゲートにする」に統合 |
| I-9 | `reviewOutcome` → `applyReviewOutcome` → `handlePluginMessage` の三段が冗長 | U-5 に既出（Design D5a）。二段→三段になったため記述を更新 |

### ユーザー判断待ち

描画層（`src/App.tsx` の配線と JSX、約115行）が完全に無検証で、`<Show when={hasRun()}>` → `when={true}` を含む6変異が生存する。完了条件4は「Review タブに表示されていないこと」という描画側の条件であり、純関数の検証では代替できない。閉じるには jsdom + `@solidjs/testing-library` の導入が必要で、これは環境変更にあたるためユーザー確認を要する。
