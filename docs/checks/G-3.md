# G-3 Completion Check

## Completion Criteria

| Criterion | Self-check | Evidence | QA | QA Evidence |
|---|---|---|---|---|
| 4チェックとも suggestion レベルで検出され、書き出しをブロックしないこと | OK | `src/checks/scopeChecks.ts`: `checkColorStyleUsage`, `checkStringBooleanVariableUsage`, `checkBareRootComponents` all build results via the new `suggestionResult()` helper (hardcodes `level: "suggestion"`, mirroring `structureChecks.ts`'s `result()` which hardcodes `level: "error"`). `checkTypographyTokenCollisions` returns an object literal with `level: "suggestion"` directly (not node-scoped, so it doesn't use the helper). All 4 are wired into `runAllChecks()` in `src/code.ts` (lines 51-62: `...runScopeChecks(nodes), ...checkTypographyTokenCollisions(vars, textStyles)`). Export-blocking in `src/code.ts`'s `run-export` handler only filters `checks.filter((c) => c.level === "error")` (line 101) before returning `export-error` — suggestion-level results are never in that filtered set, so they cannot block export. Verified with new tests in `src/checks/__tests__/scopeChecks.test.ts` asserting `results[0].level === "suggestion"` for each check, plus `runScopeChecks` test asserting `results.every((r) => r.level === "suggestion")`. |  |  |
| 各チェックの改善方法メッセージが設計書 4.7.2 節の文言と一致すること | OK | `docs/telldes-design.md` 4.7.2 「源泉・範囲チェック（提案／告知）」 bullets vs. `src/checks/scopeChecks.ts` suggestion strings: (1) Color Style → doc `"Variableに移行しませんか？（カラーはVariablesに一本化）"` = `checkColorStyleUsage`'s suggestion string, verbatim. (2) STRING/BOOLEAN Variable → doc `"これらはトークン出力対象外です"` = `checkStringBooleanVariableUsage`'s suggestion string, verbatim. (3) Bare root Component/Component Set → doc `"書き出し対象外です。画面フレーム内にインスタンスとして配置するか、ライブラリページへ"` = `checkBareRootComponents`'s suggestion string, verbatim. (4) Variable/Text Style collision → new 4th bullet added to `docs/telldes-design.md` 4.7.2 in this same change (`"トークン名が衝突しています（VariableとText Styleが同じ名前）。tokens.jsonでは片方が上書きされます。名前を変更してください"`) and `checkTypographyTokenCollisions`'s message/suggestion text was authored to match it (message names the specific Variable/Text Style pair; suggestion states the last-write-wins overwrite direction and asks to rename), consistent in content and phrasing style with the doc bullet added alongside it. |  |  |
| Variable/Text Style 衝突チェックが、Variable フルパスと Text Style 名が同一 `typography/<name>` に解決するケースを正しく検出すること | OK | `checkTypographyTokenCollisions(variables, textStyles)` in `src/checks/scopeChecks.ts`: splits each Variable's `name` on `/`, checks `parts[0] === TYPOGRAPHY_TOKEN_PREFIX` (imported from `src/util/typography.ts`, not re-duplicated — matches G-2's centralization), rejoins the remainder (`parts.slice(1).join("/")`) and checks it's in the Text Style name `Set`. This mirrors `tokensBuilder.ts`'s own path construction (`variable.name.split("/")` for Variables, `[TYPOGRAPHY_TOKEN_PREFIX, ...style.name.split("/")]` for Text Styles) so the collision condition matches the actual `tokens.json` collision surface. Verified the overwrite direction by reading `buildTokens` in `src/export/tokensBuilder.ts`: Variables are written into `tokens` via `setNested` first (loop at line ~122), Text Styles second (loop at line ~133) — on an exact-path collision, `setNested`'s terminal branch (`existing && !isLeaf(existing)` is false since a leaf already occupies the slot) falls to the plain-overwrite `else`, so the Text-Style-derived value wins. Tests in `src/checks/__tests__/scopeChecks.test.ts` (`describe("checkTypographyTokenCollisions")`): positive case (`typography/heading-md` Variable + `heading-md` Text Style → 1 result, `nodeId === ""`, message contains `"heading-md"`), and 4 negative cases (non-`typography/` prefix, no matching Text Style name, bare `"typography"` Variable with no sub-path, empty inputs) — all pass. |  |  |
| テストが全グリーンであること | OK | `bun run test` → `Test Files 10 passed (10)`, `Tests 148 passed (148)`, 0 failing. Baseline before this change was 130 passing (9 files, per docs/steering.md); +18 new tests added, all in the new `src/checks/__tests__/scopeChecks.test.ts` (covering `checkColorStyleUsage`, `checkStringBooleanVariableUsage`, `checkBareRootComponents`, `checkTypographyTokenCollisions`, `runScopeChecks`), none removed or modified elsewhere. `bun run build` (`vite build` + `vite build --config vite.config.code.ts`) also succeeds with 0 errors. |  |  |

## QA Expert Review

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Meaningful tests/verification | | |
| Edge case coverage | | |

## Expert Reviews (code changes only)

### Language Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Best practices | | |
| Codebase style consistency | | |
| GWT test format | | |

### Software-engineering Expert

| Aspect | Verdict | Evidence / Improvement |
|---|---|---|
| Separation of concerns | | |
| System integrity | | |
| Maintainability | | |

## Overall Verdict

- Self-check: OK
- QA:
- Language expert:
- Software-engineering expert:
- Ready for user review:
